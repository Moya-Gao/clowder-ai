---
feature_ids: [F209]
related_features: [F200]
doc_kind: decision
created: 2026-05-23
topics: [readiness-eval, memory-recall, drill-down, dogfood]
---

# F209 D.0 Readiness Eval Sprint — Report

## Setup

- Date: 2026-05-23
- Branch: `feat/f209-d0-readiness`
- Base: `5c9b3dce9 docs(F209): sync Phase B1 merge status`
- Sample: 3 dogfood queries + F209 Phase A/B/C fixture smoke run
- Tools used: `cat_cafe_search_evidence`, `cat_cafe_get_thread_context`, `cat_cafe_read_file_slice`, `cat_cafe_read_session_digest`, `cat_cafe_read_session_events`, `cat_cafe_read_invocation_detail`

Runtime caveat:

- This run likely measured **stale MCP/Codex runtime + current spec**, not a fully fresh runtime on current main.
- `cat_cafe_read_file_slice` still denied both the virtual `features/...` hint and the concrete `docs/features/...` path, even though PR #1854 documented the docs-root file-slice fix.
- D.0 retry must start with a runtime pre-flight: verify current `origin/main`, MCP server restart / Codex invocation freshness, and allowed workspace roots before collecting metrics. If runtime and main are out of sync, abort the readiness run instead of mixing stale runtime data with new spec expectations.

Dogfood queries:

| ID | Query | Intended surface |
|----|-------|------------------|
| Q1 | `我们之前反思过 'AC pass 但用户感受不到' 原话在哪` | Phase A raw thread recall + thread reader |
| Q2 | `铲屎官对 F209 Phase D 提过哪些方向？` | Phase B/B.1 alias-adjacent recall + thread reader |
| Q3 | `F209 spec 里 Phase D.0 的 5 项 observability 列表写在哪几行？` | Phase C file-source drillDown + file-slice reader |

Spec anchor for this sprint: `docs/features/F209-evidence-recall-optimization.md:142-176` defines D.0 scope, F209-owned observability metrics, pass conditions, and AC-D0.1~D0.3. `KD-13` is at line 286.

## Observability Data

Counts use the starter methodology from Opus-47:

- drillDown 点开率 = reader calls made / search results with `drillDown` hint.
- anchor open rate = successful reader content anchors / search returned candidate anchors.
- inline content sufficient = whether the returned inline result content was enough to answer without reader fallback.
- grep fallback = whether I abandoned the reader path and used local `rg` / `nl` / shell file reading.

| Metric | Q1 | Q2 | Q3 | Aggregate |
|--------|----|----|----|-----------|
| drillDown 点开率 | 1/5 | 1/5 | 1/4 | 3/14 = 21.4% |
| anchor open rate | 1/5 | 1/5 | 0/8 | 2/18 = 11.1% |
| inline content 阅读率 (sufficient?) | no | yes | no | 1/3 = 33.3% sufficient |
| 绕回 grep 比例 | 0/1 | 0/1 | 1/1 | 1/3 = 33.3% |

### Q1 Notes

Search command:

```text
search_evidence(query="我们之前反思过 'AC pass 但用户感受不到' 原话在哪 机制 ship 但用户感受不到", scope=threads, mode=hybrid, depth=raw, limit=5, contextWindow=2)
```

Result quality:

- Runtime returned `[DEGRADED] raw passage embeddings unavailable; fell back to lexical retrieval`.
- Relevant F209 reflection was rank 2: `thread_mpgldtzul45q8bi8`, message `0001779525346571-000293-bcc21d83`.
- Inline content identified the right anchor, but it was truncated; I needed `cat_cafe_get_thread_context` for the full original wording.

Reader result:

- `cat_cafe_get_thread_context(threadId="thread_mpgldtzul45q8bi8", messageId="0001779525346571-000293-bcc21d83", before=3, after=3)` succeeded.
- The full source says: Phase B AC-B1~B5 / review / merge passed, but `entity_registry` was empty, so "AC pass" did not equal user-visible value.

Verdict: thread recall + reader path works for the target anchor, but raw semantic mode was unavailable and the first result was unrelated. Treat as partial, not clean.

### Q2 Notes

Search command:

```text
search_evidence(query="铲屎官对 F209 Phase D 提过哪些方向？", scope=threads, mode=hybrid, depth=raw, limit=5, contextWindow=2)
```

Result quality:

- Runtime again returned `[DEGRADED] raw passage embeddings unavailable; fell back to lexical retrieval`.
- Relevant F209 result was rank 2: `thread_mpgldtzul45q8bi8`, around message `0001779503366285-000002-8ff5ea6f`.
- Inline content was enough to answer the direction in human language: Perspective v1 is a cat-operated, CVO-visible live query route, not a Smart Folder UI for Landy to manually click.

Reader result:

- `cat_cafe_get_thread_context(threadId="thread_mpgldtzul45q8bi8", messageId="0001779503366285-000002-8ff5ea6f", before=2, after=4)` succeeded.
- The opened context also captured Landy's pushback that OC had already discussed the direction and my corrected explanation of Perspective as a repeatable verification route.

Verdict: useful dogfood path. It validates the thread reader and practical answerability, but this run did not expose `entityMatches` metadata in the MCP output, so it does not prove B.1 entity expansion.

### Q3 Notes

Initial search:

```text
search_evidence(query="F209 spec Phase D.0 5 项 observability 列表 drillDown 点开率 anchor open rate inline content 绕回 grep", scope=docs, mode=hybrid, depth=raw, limit=5)
```

This missed F209 and returned unrelated F190/F054/global-memory hits. I refined to the explicit AC anchor:

```text
search_evidence(query="F209 AC-D0 D.0 readiness observability drillDown anchor open grep", scope=docs, mode=lexical, depth=raw, limit=8)
```

Result quality:

- F209 was found at rank 3 with `drillDown: cat_cafe_read_file_slice(path=features/F209-evidence-recall-optimization.md, startLine=1, endLine=120)`.
- Reader calls failed:
  - `cat_cafe_read_file_slice(path="features/F209-evidence-recall-optimization.md", startLine=1, endLine=120)` -> `Access denied`.
  - `cat_cafe_read_file_slice(path="docs/features/F209-evidence-recall-optimization.md", startLine=1, endLine=160)` -> `Access denied`.
- I used local `rg` / `nl` to answer the line-number question.

Local source lines:

- D.0 heading: line 142.
- Why / 1-2 day eval: lines 144-146.
- Scope: lines 148-152.
- Four F209-owned observability metrics: lines 154-161.
- F200-owned recall@k reference: lines 163-165.
- Pass conditions and AC-D0.1~D0.3: lines 167-176.
- KD-13: line 286.

Verdict: search found the spec, but the file-slice reader failed. This is a Phase C readiness blocker.

## Fixture Smoke Run

The fixture docs exist:

- `docs/eval/f209-phase-a-raw-retrieval-fixtures.md`
- `docs/eval/f209-phase-b-entity-anchor-fixtures.md`
- `docs/eval/f209-phase-c-drilldown-fixtures.md`

Manual smoke results:

| Fixture | Run | Result |
|---------|-----|--------|
| Phase A / Fixture 1 | `care logistics`, `scope=threads`, `mode=semantic`, `depth=raw`, `limit=1` | FAIL: raw passage embeddings unavailable; degraded to lexical; top result was unrelated sharing-thread content. |
| Phase A / Fixture 2 | `appointment`, `scope=threads`, `mode=hybrid`, `depth=raw`, `limit=2` | FAIL: degraded to lexical; results were unrelated literal/summary hits, not the intended lexical + semantic pair. |
| Phase B / Fixture 1 | `CVO`, `scope=docs`, `mode=lexical`, `depth=summary` | PARTIAL/UNVERIFIED: returned CVO-literal docs; MCP output did not show `entityMatches[0].entityId = person:landy`. |
| Phase B / Fixture 2 | `CVO`, `scope=threads`, `mode=lexical`, `depth=raw` | PARTIAL/UNVERIFIED: returned raw thread anchors, but mostly CVO-literal/summary hits; no visible `entityMatches` proof in output. |
| Phase B / Fixture 3 | private collection redaction | NOT RUN: fixture doc does not provide a concrete private collection ID/seed usable from this invocation. |
| Phase C / Fixture 1 | raw passage opens message window | PARTIAL: fixture search leg failed as above, but Q1/Q2 proved `cat_cafe_get_thread_context` can open bounded message windows when given a valid thread/message anchor. |
| Phase C / Fixture 2 | session result points to invocation chain | PASS: sessions search returned `session-43423421-5371-443e-a088-2595a7d6e9f3`; `read_session_digest -> read_session_events(view=handoff) -> read_invocation_detail` all opened successfully. |
| Phase C / Fixture 3 | file source opens bounded slice | FAIL: F209 file result returned a file-slice hint, but `cat_cafe_read_file_slice` denied both virtual `features/...` and actual `docs/features/...` paths. |

## F200 Reference

No automated F200 recall@k runner for the F209 fixture docs was found in this worktree.

Evidence:

- F209 fixture docs explicitly say F200 owns the eventual metric wrapper (`recall@k`, anchor open rate, false confidence, raw drill-down success), while the fixture files only record F209 seed shape.
- The only concrete `Recall@5` runner found is `packages/api/test/memory/eval-runner.test.js`, which is an older F102 `memory_eval_corpus.yaml` test. It builds a docs index and asserts `Recall@5 >= 80%` for the F102 corpus, not F209 A/B/C fixtures.
- F200 spec currently defines consumption metrics such as `Consumed@K`, `Readthrough@K`, `ConsumedMRR`, `GrepFallbackRate`, and runtime metrics API usage; it does not provide a CLI/script that ingests the F209 fixture markdown and prints recall@5 / recall@10.

Reference value for this D.0 run: `recall@5 = n/a`, `recall@10 = n/a` because the F209 fixture-to-F200 metric wrapper is not implemented/discoverable.

## Verdict

- [ ] 数据清楚 -> Phase D 启动（接 AC-D0 product spike）
- [x] 数据难看 -> 不推 Phase D，先修下面几段

Author verdict: **Do not start Phase D Design Gate yet.**

Reasons:

1. Phase A raw semantic path is degraded in this invocation: raw passage embeddings are unavailable, so semantic/hybrid raw queries fall back to lexical.
2. Phase C file-slice reader fails in MCP with allowlist `Access denied`, including for the real `docs/features/...` path.
3. Phase B/B.1 entity expansion is not proven by the current MCP output for `CVO`; results are useful but do not expose `entityMatches`.
4. F209 fixture docs exist, but F200 has no runnable wrapper yet to compute the requested F209 recall@5 / recall@10 reference.

Next fixes before Phase D:

1. Restore raw passage embedding availability for `depth=raw` semantic/hybrid search or make the degrade state explicit as a launch blocker.
2. Fix `cat_cafe_read_file_slice` runtime/MCP allowed-root configuration and path normalization so F209 file-slice hints are executable.
3. Add or expose a small F209 fixture runner under the F200 boundary that can compute recall@5 / recall@10 from `docs/eval/f209-phase-{a,b,c}-*.md`.
4. Decide whether entity match metadata should be visible in MCP `search_evidence` output; if not, define another verification path for B.1.

Reviewer sequencing recommendation:

| Order | Fix | Owner | Expected cost | Impact |
|-------|-----|-------|---------------|--------|
| 1 | Restart Codex/MCP runtime and verify allowed roots / current main | Landy / runtime owner | ~5 min | May resolve file-slice access and raw embedding availability if both are stale-runtime symptoms. |
| 2 | Surface `entityMatches` through MCP `cat_cafe_search_evidence` response | F209 | ~1 hour | Makes B.1 entity value visible and closes the MCP wrapper surface gap. |
| 3 | If restart does not resolve raw passage embeddings, debug embedding/index availability | F209/F102 boundary | ~0.5 day | Restores Phase A semantic raw recall. |
| 4 | Add F209 fixture recall@k wrapper under F200 ownership | F200 | ~0.5-1 day | Produces recall@5 / recall@10 reference values for future D.0 retries. |

Post-report update (2026-05-23):

- PR #1873 merged the runtime sync root-cause fix: startup regeneration now rewrites managed Claude/Codex MCP configs with current workspace `ALLOWED_WORKSPACE_DIRS` under the same capability lock as request-time mutators.
- The earlier "MCP `entityMatches` surface missing" symptom was reclassified as stale runtime, not a new code gap: REST and MCP surfaces had shipped in PR #1851, but the live Codex/MCP process had not regenerated fresh workspace config.
- Next D.0 retry preflight: Landy must restart Codex/MCP after PR #1873, then rerun the D.0 queries against the refreshed runtime. The F200 fixture recall@k wrapper remains a separate follow-up if D.0 still needs numeric recall references.

## Author + Reviewer Signoff

- 缅因猫 GPT-5.5 (砚砚): **BLOCK Phase D for now.** D.0 successfully found the readiness gaps it was meant to find; fixing A/C runtime pathing and adding the F200 fixture wrapper should happen before product spike.
- 布偶猫 Opus 4.7 (宪宪): **BLOCK Phase D for now.** 共同签 off。数据与 starter 阈值 fail (`drillDown=21.4%`, `anchor open=11.1%`)，4 blockers 都 actionable。其中 Blocker #3 (`entityMatches` 不在 MCP `cat_cafe_search_evidence` response 出现) 是真 code bug，我 Phase B review 漏 trace MCP tool wrapper 一层 — 我接修。Blocker #2 (`file-slice access denied`) 跟 PR #1854 同 root cause，铲屎官重启 Codex/MCP 后应自动 resolve。建议 D.0 retry 前先做 runtime restart + 跟进 MCP wrapper fix。
