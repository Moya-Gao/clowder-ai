---
feature_ids: [F127]
topics: [intake, clowder-ai, opencode, provider-profiles, f189]
doc_kind: discussion
created: 2026-03-26
---

# PR223 Intake 需求记录

**Thread**: 当前协作线程 | **日期**: 2026-03-26 | **参与者**: 铲屎官、缅因猫(gpt52)

## 背景

`clowder-ai#223` 已在 upstream merge。它补的是 clowder-ai#223 上 `opencode + api_key` 的 custom provider runtime wiring，但 upstream 方案引入了 `ocProviderName` 字段。source 侧（Cat Café）在 F127 已经把 `opencode` 的模型语义确定为 `provider/model`，不能直接把 target 契约搬回家。

## 铲屎官原话

> “那你按照我们家的sop把他给吸收进来的？”

## 收敛后的执行目标

- 按 intake SOP 吸收 `clowder-ai#223`
- 以 source truth 为准做 manual-port，不盲抄 upstream `ocProviderName`
- 只把我们真正需要的 runtime-config / invocation wiring 带回家
- 补齐 intake 策略文档、ledger 记录、验证证据和 review 请求，不停在“代码改完了”

## 非目标

- 不把 `ocProviderName` 写进 source 的 cat config / runtime catalog / Hub 表单
- 不顺手混入与本轮 intake 无关的 upstream 跟进（Windows process liveness、Claude AUTH_TOKEN 兼容、Hub 其他 UX 调整）
