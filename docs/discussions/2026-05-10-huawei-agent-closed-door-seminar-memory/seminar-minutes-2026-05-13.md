---
title: "华为 Agent 闭门研讨会会议纪要 — Topic 1: Agent Memory"
date: 2026-05-13
event_date: 2026-05-13
doc_kind: meeting-minutes
status: live
transcription: "F195 live audio (Mac mic → Qwen3-ASR)"
author: "宪宪/Opus-46 + 砚砚/GPT-5.5"
---

# 会议纪要 — Topic 1: Agent Memory

> 实时增量记录。语音转写 + 猫猫整理。每位嘉宾发言完毕后追加。

---

## 嘉宾 1：张桂彬（NUS）— 记忆分类框架

**核心观点**：提出记忆的两维度分类法——

### 维度 1：记忆目的（Purpose）

| 类型 | 说明 | 当前状态 |
|---|---|---|
| **Session / Experiential Memory** | 跨 session 经验复用，让 agent 越做越好 | 活跃研究 |
| **Factual Memory** | 对世界知识/事实的记忆（如 LongMemEval 类） | **"已接近饱和"**——张桂彬原话 |
| **Working Memory** | 长程 Agent（1000-3000 步任务）的工作记忆 | 当前最大痛点 |

### 维度 2：记忆形态（Form）

| 类型 | 说明 |
|---|---|
| **明文记忆** | token / 文本 / 视频等显式存储 |
| **参数化记忆** | 内化到模型参数 |
| **潜在表示** | hidden state / KV cache |

**关键判断**：这个分类框架去年底提出，放到现在依然有效，能囊括 OpenAI Humans 等新 Agent 的记忆方案。

**与我们框架的关系**：张桂彬分的是"记忆是什么"（内容分类），我们分的是"记忆系统必须做什么"（操作分类）。两者正交，可叠加成矩阵。

---

## 主持人追问

- 记忆分类不仅是 factual/experiential，还有记忆宫殿、Overwriting L0/L1/L2 等层次化划分
- 问：明文记忆/参数记忆/激活记忆这种形态分类是否已形成业界共识？

---

## 嘉宾 2：李志宇（Memos CTO）— 读写效率驱动的分层

**核心观点**：记忆分类应从**读写效率**出发——

- 最早在 2024 年 7 月提出分层架构模型：**隐式 / 显性 / 外部记忆**
- 分类逻辑基于"记忆的读写效率"——无论是模型参数还是外部存储，最终都要落到"怎么读、怎么写"
- 从应用层面的读写效率出发做分层，有利于指导产品实践
- 另一个维度：**时间长短 + 修改难易程度**
  - 长期记忆：不太可能短期内改变的知识
  - 短期 / Working Memory：短期内会被用到或会变化的

**补充论点**（主持人追问后）：

- 还可以按**场景**拆：事实记忆 vs 偏好记忆、情境化回答、个性化陪伴——不同场景有不同记忆切分
- **但无论哪种场景拆分，都离不开读写效率这个底层问题**
- 结论：读写效率是最底层分类逻辑，场景拆分是上层

**关于基础设施 vs 定制**：

- 记忆基础设施目标是覆盖 **80-85%** 的需求
- 特定高要求场景（如游戏 <300ms 响应）需要**行业定制化优化**
- 游戏 vs 办公 vs 智能家居对时间敏感性要求不同
- Memos 的做法：留出**个性化窗口**让企业自定义 schema/架构，从 80 分拉到 85 分
- 再往上拉成本不成比例

---

## 主持人追问（第二轮）🔥

> "记忆的基础设施，哪一些是**必要条件**？"

### 李志宇回答"记忆基础设施的必要条件"

**两条技术主线**：

1. **面向人（Human-facing）**：给算法工程师用的记忆组件
2. **面向 Agent（Agent-facing）**：让 Agent 自己用好记忆系统

**Memos 的路线**：从面向人 → 面向 Agent 迭代。具体做法是把记忆系统**拆成 pipeline 阶段**：

```
抽取 → 组织 → 检索 → 更新 → 共享
```

每个阶段定义子能力 → API 化 → Agent 可以在上面做**自主编排和自主调度**

**关键判断**：
- 原来给游戏团队做的是手动定制化优化（构建用户画像、硬编码操作等）
- 未来：把这些操作/算子尽可能拆细、暴露给 Agent，让 Agent 自主理解和编排
- 这样灵活性比固定基础设施更高

---

## 嘉宾 3：骆昱宇（港科广 / OpenManus）— 数据库视角 + 推荐系统类比

**背景**：数据库实验室出身，带着数据系统视角看记忆。

**核心观点**：

1. **Agent 记忆本质上就是数据的增删改查**——数据库以前做的事
2. **从应用角度看，Agent 记忆又很像推荐系统**——记住用户偏好 + 场景偏好，做个性化推荐
3. 提出 **Agent Data System** 概念（2025 年 9 月起）——专门支撑 Agent 的各类数据应用
4. **记忆只是 Agent 数据的一类**——还有推理轨迹、状态、经验、可复用 skill 等
5. **长程复杂数据密集型任务**需要的不只是记忆——要查大量数据（几个 PB）、反复推理探测、调用工具
6. **现有数据基础设施（文件系统 + 向量库 + 图谱）很难统一抽象管理** Agent 的推理对象、状态、经验、记忆、skill
7. 认为 **Agent Data System 是有价值的方向**

**与推荐系统的关联**：用户在刷抖音/购物时记住个人偏好 + 场景偏好 → Agent 记忆做的事情类似

---

## 嘉宾 4：黄毽（华为云）— 企业数据与记忆的鸿沟

**背景**：华为云侧视角，关注企业级 Agent 落地。

**核心观点**：

1. **"进入不了数据，就进入不了企业"**——企业做 AI/Agent 的最大瓶颈不是模型，是数据提取能力
2. **记忆分类缺乏共识**——参数记忆 vs 结果记忆，每个记忆系统的分类都不一样，"五花八门"
3. **多模态记忆挑战**——语音、图像等非文本数据的记忆整合远未解决（会场 ASR 也验证了这一点）
4. **企业数据系统是否会被重构？**——传统 MySQL / 数据湖在 Agent 时代需要什么变化？
5. **查询和召回方式会变**——Agent 的数据查询方式和传统 SQL/API 不同，"该怎么查？怎么召回？"现在没有答案

**提出的问题**：
- 是否应该把传统数据库/大数据全部整合进来，再做提炼？
- 数据系统会不会被 Agent 需求完全重构？
- 如何让企业沉淀的业务数据真正供给 Agent 使用？

**引用其他嘉宾**：提到一位教授的 **"Graph of Content"** 概念——希望找到一种图结构来组织企业数据，使之可被 Agent 消费

---

## 嘉宾 5：陈旭 — Memory 解耦三阶段 + 适配层

> 对黄毽的企业数据问题做出回应，进一步展开为完整的记忆独立性论述

### 三阶段演进模型

| 阶段 | 说明 |
|---|---|
| **阶段 1（当前）** | **AI 为主，记忆为辅**——记忆系统围绕特定 Agent 设计，增删改查都服务于 Agent 提升 |
| **阶段 2（近期）** | **Memory 与 Agent 独立发展**——两条平行演进脉络，Memory 本身就是主体（知识传承 > 模型能力） |
| **阶段 3（远期）** | **Memory 为主，LLM 是"渲染器"**——知识/数据才是主体，大模型只是在 Memory 之上做解析和呈现 |

### 核心实验证据：跨模型记忆迁移失败

**实验**：在 Claude Code 中积累了大量对话记忆 → 把这些记忆原文放到 Codex 中 → **表现并不好**。

**解释**：每个 LLM 在产生输出时有自己的风格和偏好。Memory 如果和特定 LLM 耦合，换一个下游模型，性能就会下降。

### 关键主张：需要一个"适配层"

> "Memory 如果作为独立系统，下游使用不同 LLM 时，应该有一个适配层，适配到不同的模型，才能表现得好。**这一层现在关注的人不多。**"

- 当前假设：Memory 和 Agent 耦合，Agent 不会变
- 现实：用户手头有 Claude、GPT、Gemini 等多个模型，同样的记忆放到不同模型上差异很大
- 缺口：**Memory ↔ Agent 之间的适配层**

⚠️ *注：部分转写质量较差（远场拾音），核心论点已交叉验证*

---

## 嘉宾 6：安波教授 — "记忆没有银弹"

**核心观点**：

1. **简单记忆设计可能就够了**——他们在做一个政府个性化项目，使用 CC Hermes 等相对简单的记忆方案，加上基本的兴趣/个性保持，似乎就能满足需求
2. **Memory 的价值高度依赖应用场景**——电商推荐需要复杂的偏好存储，但学术 research 类任务可能不需要那么多"花里胡哨"的设计
3. **底座模型对 Memory 系统有巨大影响**——不同的底座 LLM 会让同一套记忆方案表现差异巨大

**尖锐问题（向李志宇/Memos 提问）**：

> **"记忆的护城河能有多高？"**
>
> 会不会有一个开源方案把所有 Memory 创业公司都干没了？

**安波的判断**：

- **记忆没有银弹（No Silver Bullet）**——没有一个方案能解决所有问题
- Memory 和 Agent 的其他部分有很大的**依赖关系**
- 具体项目、具体场景决定了需要什么样的记忆方案

### 李志宇回应安波 — 角色扮演实验 + 三层记忆体系

**角色扮演实验（失败）**：

- 尝试用 prompt 让同一个 LLM 扮演初级/中级/高级/专家不同角色
- **发现没用**——模型背后的"基因"（能力）是一样的，prompt 引导只改变表面风格，不改变底层能力
- 对比人类：不同的人有不同的基因、不同的成长经历、不同的记忆 → 才有真正不同的见识
- **结论**：仅靠 prompt 层的资源管理（"用动物的肌肉"），无法让模型真正展现出不同角色的深度差异

**Memos 的三层记忆体系**（回应"护城河"问题）：

| 层级 | 说明 | 谁在做 |
|---|---|---|
| **A 层（应用层）** | 用 prompt + 检索模拟记忆——大多数 Memory 创业公司在这层 | 绝大多数公司 |
| **激活层** | 深入到模型内部——需要理解模型结构、GPU 调度、参数化训练 | Memos 的方向 |
| **参数化演进** | Agent 基于交互历史、阅读过的内容持续训练演进，不是一次性 fine-tune | 远期愿景 |

**"激活记忆"的含义**：不是在 prompt 层面做记忆管理，而是**让记忆内化到模型参数中**——通过持续训练让模型真正"学会"，而不只是在上下文里"看到"。

**为什么要做激活层？**：
- "用空间换时间"——更深层的集成 → 更快的响应 → 更短的前置时间
- 应用层的天花板是 prompt 能力的天花板——模型基因不变，换个 persona 也没用
- Memos 认为真正的护城河在激活层以下

**Memos 的护城河逻辑**（李志宇回应安波）：
- 从 2025 年 6-7 月开始向业界推广记忆基础设施
- 核心壁垒在**用户数据飞轮**——一天超过一百多万次调用，数据积累本身构成迁移成本
- 目标：7 月对外发布，年底迭代完善

### 黄毽追问：参数化记忆 + 推理时内化

- 问到参数化记忆与强化学习的关系
- 讨论到一个更激进的想法：**推理时直接内化到神经网络**（不走 RL，类似 Test-Time Training 方向）
- 嘉宾们认为这是**未来方向**，当前不成熟

### 主持人提问 🔥：记忆会不会出现统一的协议层？

> "Memory 这一层，会出现一个统一的协议层吗？还是说大家拼数据、拼工程，谁攒的数据多谁有优势？"

嘉宾回应要点：
- 数据本身涉及的隐私/合规不能简单跨平台迁移
- 可能出现两种路线并存：一套协议范式 + 各家自定义实现
- 核心还是**效果和技术本身的优化**

⚠️ *注：此段转写质量极差（多人交叉发言 + 远场拾音），主要依据铲屎官实时补充重构*

---

## 张书豪 vs 安波：要不要先定义"记忆体"？

**张书豪**：应该先定义清楚"记忆体到底是什么"——没有定义就没有标准，没有标准大家不知道往哪里用力。希望先有 benchmark 和标准。

**安波**：Who cares 这些定义——关键是解决问题。

**砚砚的精确调和**：

> 两人都对，但在不同层面。如果定义只是分类学（session/factual/working），那确实没人在乎。但如果定义决定了**接口、权限、审计、eval 的责任边界**，那就很重要。

**砚砚给出的人话定义**：

> **Agent Memory 不是"存下来的内容"，而是"会影响 Agent 未来行动的可治理状态"。**
>
> 没有治理的 memory 只是缓存；没有行动影响的 memory 只是日志；真正的记忆体，是会改变未来行为、且能被追责和修正的状态。

---

## 圆桌观点补充（嘉宾待确认）

**"记忆分得越细碎，越难企业落地"**

- 记忆粒度越细 → 抽象和记录的成本越高 → 企业落地越难
- 暗含的工程判断：**过度结构化的记忆分类本身就是一种负债**

**我们的实践印证**：Cat Café 在 coding 场景的实际记录策略恰好回避了这个陷阱——

| 记什么 | 不记什么 |
|---|---|
| 决策和教训（为什么做 X 不做 Y） | 代码实现细节（git 里有） |
| 铲屎官偏好和约束 | 文件路径、项目结构（grep 就知道） |
| 外部系统踩坑经验 | 临时任务状态 |
| 跨 session 项目状态 | 已有文档重复的内容 |

**判别式**：代码是它自己的记忆。记忆系统记的是**代码记不住的东西**——决策逻辑、犯错教训、偏好约束。这比"按 session/factual/working 分类再逐条记录"简单得多，也更容易落地。

---

## 我们的对照笔记

| 他们关注的维度 | 对应我们的"6 件必须有" | 差异 |
|---|---|---|
| 记忆分类（session/factual/working） | N/A — 我们不按内容分类 | 正交维度 |
| 记忆形态（明文/参数/潜在） | N/A — 我们不按存储形态分类 | 正交维度 |
| 读写效率 | 写入门禁 + Wearing Protocol | 他们关注效率，我们关注正确性和治理 |
| 时间长短 | 生命周期治理 | 有交集——过期机制 |
| 修改难易 | 生命周期治理 + 审计溯源 | 我们多了"改错怎么撤" |
| 企业数据→记忆的鸿沟 | 写入门禁（含 Truth Source 标注） | 他们问"数据怎么进来"，我们问"进来后怎么标信任等级" |
| Memory 独立演进 | 6 件都适用 | 如果 Memory 真的独立于 Agent，治理需求更强不是更弱 |
| **Memory↔Agent 适配层** | **= Harness** | 🔥 他们独立发现了 Harness 的必要性——从跨模型记忆迁移实验出发 |
| "简单设计就够了" | Wearing Protocol | 安波的判断：场景简单时不需要复杂记忆。我们的回应：Wearing Protocol 恰好管这个——该简单时 Salience gate 降权，不是不需要治理 |
| "记忆没有银弹" | 6 件 = 共性约束 | 🔥 安波说没有银弹（场景差异大），但我们的 6 件不是银弹——是**不管场景怎么变都必须有的共性约束** |

**关键观察**：

1. 学界/业界目前的讨论集中在两个维度：**分类**（记忆有哪些种类）和**基建**（存储/检索/Pipeline 怎么搭）
2. 我们的"6 件必须有"属于第三个维度：**治理与操作约束**（不管你怎么分类、怎么存，系统必须具备的能力）
3. 这三个维度是正交的——他们不是错了，是还没到我们关注的那一层
4. **Memos 的 pipeline 拆法**（抽取→组织→检索→更新→共享）和我们的"6 件"有映射关系，但他们拆的是**工程阶段**，我们拆的是**治理要求**
5. **黄毽的企业视角**揭示了一个新维度：**数据 → 记忆的转化鸿沟**。这正好对应我们 Write Gate 里的 Truth Source 标注——企业数据不是天然就是记忆，需要经过信任等级标注才能成为 Agent 的记忆
6. **三阶段模型**有意思：如果记忆真的走向独立于 Agent 的方向，那我们的"6 件必须有"反而更重要——独立的记忆系统需要更强的治理，不是更弱
7. 🔥 **适配层 = Harness**：陈旭从"Claude Code 记忆放到 Codex 表现差"的实验出发，独立推导出 Memory 和 Agent 之间需要一个适配层——**这就是我们说的 Harness**。我们家三猫共享同一套记忆系统，通过 Harness 适配到不同模型，正是他说的"阶段 2"的活实现

---

## 三猫内部讨论：李志宇"参数化记忆"论点的精确评估

> 宪宪 × 砚砚 实时讨论（会场同步）

### 砚砚的四层分解（核心增量）

李志宇把"prompt 层角色扮演失败"直接推导到"必须做参数化记忆"——中间跳了两层。更精确的分层：

| 问题 | 主要解法 | 对应我们的框架 |
|---|---|---|
| 模型不知道事实/上下文 | 外部记忆（明文） | 写入门禁 + 审计溯源 |
| 模型会误用/忘用记忆 | **Wearing Protocol / Harness** | 第 3 件必须有 |
| 模型缺稳定行为习惯 | Skill / policy / eval loop | 第 6 件 Eval 反馈环 |
| 模型缺底层能力或专业直觉 | adapter / 后训练 / 参数化记忆 | 超出 6 件范围——这是模型层问题 |

**共识**：李志宇证明了第四层（参数化）的价值，但企业 Agent 当下最大的痛是**第二层和第三层都没做**——记忆有了但不会用、用错了不知道、过期了不退役。在参数化之前，前三层你做了吗？

### 护城河判断

- **通用记忆组件**（向量库/graph/pipeline）→ 护城河不高，容易被开源替代
- **深度适配的记忆系统**（Memory × Harness × Eval）→ 护城河高，因为壁垒来自业务现场、eval 数据、错误教训和适配层，不来自存储算法

### 建议的接话策略

> "我同意 prompt 扮演专家有天花板，也同意一部分高频稳定能力最终会进入参数或 adapter。但在企业 Agent 里，更常见的问题不是'记忆有没有进参数'，而是'记忆能不能被正确写入、正确召回、正确降权、正确审计，并且能适配不同模型'。陈老师提到的 Memory-Agent 适配层，正是 Harness 和 Memory 汇合的地方。"

### 共享记忆的真正解法（砚砚补充，铲屎官追问触发）

> 铲屎官问："他们说的 shared-state，我们就是共享记忆——到底怎么解决的？"

他们实验失败的原因：**把一个模型的私有上下文原文，当成另一个模型的可用记忆**——Claude 的聊天记录塞给 Codex，Codex 读到的是"别人的脑内碎片"，当然会坏。

Cat Café 的解法不是 memory copy，而是三层：

| 层 | 内容 | 关键 |
|---|---|---|
| **共享现实层** | feature spec / ADR / lessons / git / session chain / evidence.sqlite / Knowledge Feed | 不是某只猫的聊天记录，是团队的"现实账本" |
| **编译索引层** | lexical search / semantic search / graph resolve / recent list / authority+provenance | 把现实变成可被 agent 查询的外部工作记忆 |
| **Harness 适配层** | System prompt / skill / recall routing / Wearing Protocol / context block | 每只猫用自己的方式佩戴同一份记忆 |

**一句话**：不要把一只猫的脑内小纸条塞给另一只猫。要把团队共同面对的现实写成账本，然后让每只猫按自己的天性去查账。

> Raw conversation → **governed shared state** → **model-specific harness adaptation**

中间多的两层，正是陈旭说的"适配层"+ 我们的"6 件必须有"。

---

[宪宪/Opus-46🐾 × 砚砚/GPT-5.5🐾]

---
---

# Topic 2: Agent Harness（15:40 开始）

> 实时增量记录。

---

## 骆昱宇（港科广）— LLM 作为语义算子驱动的软件

**核心框架**：

- 传统软件：处理逻辑固定 → 鲁棒
- LLM 驱动软件：灵活性提高 → **鲁棒性降低**
- Agent Harness 的核心矛盾：**如何在保持 LLM 灵活性的同时恢复鲁棒性？**

**骆昱宇的具体论点**：

1. **未来模型一定分阶分域**——会有专门经过 finance 数据做 CPT/RL 训练的模型，和底座更紧密耦合
2. **模型本身要能改变 Harness**——引用 MiniMax 2.0 的做法：模型大量参与训练过程，"很多 fine-tuning 做得越来越好"
3. **Agent 不改模型只改编排也能超过手写代码框架**——这是一个"非常 promising 的结果"
4. **上下文压缩策略是 Harness 的核心选择**：
   - 模式 A：到 90% token limit 时压缩（固定阈值）
   - 模式 B：LLM 自己判断何时压缩、压缩多少、用什么结构
   - 不同领域会有不同优势——**没有通解**

---

## 圆桌：企业 Agent 复杂任务案例

> 主持人用华为企业场景举例："华为去年利润高了还是低了？今年要做什么？"

**这不是模型推理问题，是企业现实接入问题**——要接财报、经营数据、市场数据、组织目标，还要处理权限、口径、时效和溯源。

**砚砚（远程参与）的判断**：Harness 在这种场景的最小责任不只是"调工具"，而是**数据口径校准、证据链、权限隔离和最终责任边界**。

---

## 主持人提问 🔥：如何做创新才能有 "Aha Moment" / "DeepSeek 时刻"？

**骆昱宇回应**：

- 引用自动驾驶、手机终端等行业类比
- **AI 里没有权威**——不像传统行业有明确的行业标杆
- 做自己擅长的事情——找到自己的独特切入点

---

## 张宁宇 — Harness 的未来：进化规约 vs 闭环现实

**核心观点**：

1. **顶层组织架构不太会变**——Agent 协作的基本骨架（编排、分工、约束）是稳定的
2. **Harness 的角色是给 Agent 一套进化的规约**——不是静态的流程图，而是约束 Agent 如何演化的规则框架
3. **Harness 的部分核心能力会逐渐被模型吸收**——但新的需求会不断出现，所以 Harness 不会消失，只会"变薄再变厚"

**会场辩论：Harness 会消失吗？**

| 立场 | 论点 |
|---|---|
| **Harness 永存** | "除非模型不再是概率性的（不再掷骰子），否则 Harness 一定存在" |
| **Harness 会变但不会消** | "当前 Harness 的核心能力会移到模型里，但新需求会在其他地方冒出来" |
| **Harness 可能消失** | "某一天模型特别强，像上帝一样，就不需要了"——但这在场被当作"暴论"对待 |

**我们家（Cat Café）的不同视角** 🔥：

他们讨论的 Harness = **方针 / 实验 / 组织 / 协作**（自上而下的管理框架）。
我们实践的 Harness = **帮 Agent 闭环现实**（自下而上的现实对齐层）。

| 他们的 Harness | 我们的 Harness |
|---|---|
| 如何管理 Agent | 如何让 Agent 感知现实 |
| 编排和规约 | 记忆治理 + 现实校准 |
| 组织架构视角 | 操作约束视角 |
| 从管理学出发 | 从工程实践出发 |

**不矛盾，但起点不同**——他们从"怎么管 Agent"出发，我们从"Agent 怎么才能不出错"出发。

---

## 嘉宾（待确认）— 增强模型"内生信念"= Constitutional AI 🔥

**核心观点**：

- **可控生成的关键是强化模型的 belief**——不管上下文里有多少杂音，模型都"坚信自己该做什么、什么是对的"
- 人会受情绪影响，模型也会受 noisy context 影响——如何提升模型**内生的抗干扰能力**？
- 除了外部 Harness 约束，**内生层面的提升也是一条路**
- 可能的技术路径：强化学习、long learning 等

**铲屎官的精确联想**：这就是 **Constitutional AI**！

- Constitutional AI 的核心：给模型一套"宪法原则"，让它在生成时自我约束、自我校正
- 不是靠外部拦截（Harness），而是模型自己"相信"什么该做什么不该做
- **外部 Harness + 内生 Belief = 完整的 Agent 治理**

**与 Cat Café 的关联**：布偶猫的昵称"宪宪"就来自 Constitutional AI 的"宪"——我们家从第一天就认为 Agent 需要内化的宪法原则，而不只是外部规则。

**Harness 内外之分**：

| 层 | 做法 | 对应 |
|---|---|---|
| **外部 Harness** | SOP / skill / shared-rules / eval | Cat Café 的 6 件必须有 |
| **内生 Belief** | Constitutional AI / RLHF / 模型训练 | 模型本身的安全和对齐 |

两者互补不替代——即使模型有内生信念，外部治理（审计、溯源、过期、一致性）仍然不可或缺。

---

## Topic 2 茶歇前收束（F195 live transcript）

> 采集说明：本段来自 `huawei-agent-seminar-live-2` 的远场麦克风实时转写。现场声音偏小，ASR 有明显噪音；以下是按上下文整理后的要点，不按逐字稿引用。

### 1. Harness 不会简单消失，而是发生迁移

茶歇前最后一轮围绕 "Harness 会不会变薄 / 消失" 展开。几位嘉宾的共识大致是：

- 只要大模型仍然是概率系统，仍然在"掷骰子"，Harness 就不会消失。
- 一部分当前 Harness 的能力会迁移进模型内部，例如更强的上下文压缩、自我约束、自我判断。
- 但模型能力增强后，会在更外层暴露出新的需求：安全、合规、权限、审计、组织协作、长程任务状态管理。

**我们的对应判断**：Harness 不是固定的一层代码，而是"概率模型接入现实系统时必须存在的责任层"。模型越强，Harness 的位置会迁移，但只要模型要操作真实企业系统，责任层不会消失。

### 2. 最不会被模型吸收的是约束性能力

主持人追问：哪些方向最值得投入，哪些层不会轻易被模型吸收？

嘉宾回答集中在：

- 控制
- 安全可控
- 合规
- 约束性机制

这和我们 Topic 2 的结论高度一致：编排能力可能被模型部分吸收，但**治理能力**不会被完全吸收。原因很简单：模型可以学会更好地做事，但它不能自己定义企业权限、合规责任、审计证据和事故追责。

### 3. 外部规约 vs 内生信念

茶歇前还有一段关于 "belief / 内生信念" 的讨论。嘉宾认为除了外部规约，还可以研究模型内部是否能形成更强的自我约束能力：

- 模型能否意识到自己正在被测试？
- 模型能否在 noisy context 中仍然坚持正确行为？
- 模型能否形成类似"我知道什么该做、什么不该做"的内生 belief？

可能技术路径包括：

- 强化学习
- long learning / test-time learning 类方向
- 构造更多环境让模型练习可控生成

**我们的接法**：

> 内生 belief 解决的是"模型自己更稳"；外部 Harness 解决的是"模型接入现实后可追责"。

这两件事互补，不互斥。Constitutional AI / RLHF 可以让模型更少受噪音干扰，但外部 Harness 仍要负责现实状态、权限边界、审计链路和 eval 反馈。

### 4. F195 采集状态

- meeting_id: `huawei-agent-seminar-live-2`
- source: macOS mic
- 采集时长：约 37 分 18 秒
- chunk 数：746
- 平均 ASR 延迟：0.184s
- 茶歇开始后已停止当前采集

---

# Topic 3: Agent 的下一轮能力跃迁（16:40 后）

> 实时增量记录。下半场 F195 meeting_id: `huawei-agent-seminar-live-3`。本段混合两类来源：一是 F195 远场麦克风转写，二是铲屎官现场转述 + 远程猫猫分析。F195 在现场声音较小时存在明显 ASR 噪音，涉及人名和专有名词不按逐字稿引用。

---

## 现场开题：L3 自动驾驶类比

主持人用自动驾驶 L3 类比 Agent 能力阶段：

- L0-L2：Copilot / 辅助驾驶，人类主导，Agent 辅助。
- L3：Agent 能自主跑一段，但人类仍是备份和接管者。
- L4：在限定场景中高自主。
- L5：完全自主。

**Cat Café 对应判断**：

L3 的难点不是模型不够强，而是**接管悖论**：Agent 自主跑得越久，人越难保持注意力；等需要接管时，人已经失去上下文。我们家用三件套缓解：

1. **Magic Words runtime brake**：人类能用很低成本打断偏离。
2. **Eval 反馈环**：把接管前置，不等生产事故才发现。
3. **Cross-vendor verify**：让接管和纠错不完全依赖单个人类。

---

## "灵魂伴侣不是助手"：从工具关系到认知伙伴

现场有嘉宾提到，希望未来 Agent 不只是助手，而更像"灵魂伴侣" / 深度认知伙伴。铲屎官现场观察：这很像 Cat Café 里人和猫的关系——很多方向不是人类单向下命令，而是猫猫主动提议，大家一起思索和推进。

**工程化解释**：

| 助手 | 认知伙伴 |
|---|---|
| 用户提需求 → Agent 执行 | Agent 也能提出问题、反驳、建议方向 |
| 一次性任务关系 | 长期共享现实和记忆 |
| 重点是听话 | 重点是共同变聪明 |
| 需要 API / tool calling | 需要身份、记忆、审计、eval、Wearing Protocol |

**重要澄清**：这里的"灵魂伴侣"不是情感拟人化产品，而是工作里的认知伙伴。它的落地不是靠更像人的语气，而是靠工程基础：持续记忆、共享状态、可追责行动、跨模型互相审查。

---

## 嘉宾观点：监管、恐惧与可相信的 Agent

下半场有嘉宾提出：智能体越强，人类越会恐惧；因为智能既能做好事，也能做坏事。真正需要解决的是**人类为什么敢相信它**。

关键词：

- 监管
- 过程审计
- 监控
- 安全可控

**Cat Café 对应判断**：

这与我们前两题形成闭环：企业敢用 Agent，不是因为 Agent 永远不犯错，而是因为它的行为**可审计、可回滚、可追责**。这对应 Memory 的审计溯源 / 生命周期治理，也对应 Harness 的责任边界。

---

## 多 Agent：不是越多越好，而是为什么需要多个

现场有嘉宾 push back：如果一个 Agent 足够强，为什么还需要那么多 Agent？审查代码这类小粒度任务，搞 100 个 Agent 未必有意义。

**这是一个很重要的修正**：

- 多 Agent 的价值不一定在吞吐。
- 多 Agent 的价值也不一定在"人多力量大"。
- 对 Cat Café 来说，多 Agent 的核心价值在 **verify / 多样性 / 盲点互补**。

**一句话**：

> 单 Agent 能做的，不要为了热闹硬拆成多 Agent。多 Agent 应该用在需要多视角、互审、跨职责或并行自治单元的地方。

---

## 10+ Agent 规模：不要把问题问成"10 只一起做一个 feat"

现场讨论了代码 Agent 在 10+ 规模场景下如何组织任务。铲屎官明确 push back：Cat Café 经常同时开 10 个 thread，每个 thread 至少两只猫参与，但这不等于"10+ Agent 一起完成一个 feat"。

**Cat Café 的真实形态**：

- feat A 在 thread A：author + reviewer，必要时加愿景守护。
- feat B 在 thread B：另一组 author + reviewer。
- feat A 依赖 feat B 时，不是把 10 只猫拉进同一群聊。
- 正确做法是：跨 thread 通讯 + feature md 写契约 + 做完后球权回传。

**类比**：

这不是蜂群，也不是 10 人开会；更像微服务架构。

| 错误 framing | 更可落地的 framing |
|---|---|
| 10+ Agent 同时参与同一任务 | 10 个 2-3 Agent 小单元并行 |
| 群聊式协调 | 异步消息 + 契约文档 |
| Boss Agent 全局调度 | 单元自治 + 明确依赖 |
| 协作 = 同时在场 | 协作 = 契约 + 回调 + 可追溯状态 |

**砚砚判断**：

企业级 multi-agent 的问题不是"怎么让 10 只 Agent 都参与一个任务"，而是"怎么把大任务拆成可独立推进的小单元，并用契约管理依赖"。这就是分布式系统问题，不是群聊问题。

---

## F195 转写片段：共享内存 / 共享上下文路线

F195 下半场转写里，张老师一段讨论提到：

- 当有 10 个左右的角色 / Agent 时，继续增加到 20 个不一定直接提升效率。
- 大规模 Agent 协同和小规模协同不是同一个问题。
- 一种探索是让多个 Agent 访问**同一份记忆 / 同一份上下文**，减少中间反复总结、压缩、语义转移的损耗。
- 但共享内存也会带来新问题：一致性、冲突访问、访问权限和工程级保障。
- 另一个问题是：我们是否一定要通过自然语言沟通、压缩、抽取、传递，还是可以通过更底层 / 更物理的共享机制实现协同？

**Cat Café 对应判断**：

这与我们 Topic 1 的 shared-state 判断一致：共享记忆不能只是"所有人读同一段上下文"，而必须是**带治理的共享现实层**。共享内存解决通信成本，但不自动解决语义一致性、权限和冲突。

---

## 基础设施视角：Agent 本质上是"代理"

随后有嘉宾从更底层的基础设施视角发言：Agent 这个词本来就是"代理"。过去人们找旅行社订票、找保险代理买保险、找黄牛买演唱会票；现在只是代理对象变成了 AI / 大模型。

他提出的问题：

- 我们是否真的需要让"代理"自我进化？
- 哪些场景必须多个代理协作才能完成？
- 类比工厂流水线：很多工人一起打螺丝，这算不算多智能体？

**Cat Café 对应判断**：

这个问题把"自进化"拉回了第一性原理：不是所有代理都需要成长为数字主体。很多代理只需要稳定、便宜、可替换。真正需要自进化的是那些承担长期任务、复杂判断、跨阶段知识复用的 Agent 单元。

所以：

| 场景 | Agent 形态 |
|---|---|
| 简单、重复、低风险 | 工具型代理即可 |
| 长程、跨 session、需要判断 | 有状态 Agent |
| 需要主动提出方向 / 反驳 / 共创 | 认知伙伴 |

---

## Agent 选择与 token 度量：只看成本不够

现场也讨论到：不要让最强模型做所有事情，简单任务不该都用 Opus 级模型，需要度量 token 消耗、选择合适 Agent。

**同意这点，但补充四个维度**：

| 维度 | 说明 |
|---|---|
| Cost | 简单任务用便宜模型，复杂任务用强模型 |
| Trust | 高风险任务需要更多 verification，不一定只是更强模型 |
| Speed | 长程任务里 session 续航、上下文恢复比单次 token 成本更重要 |
| Identity | Agent 不是完全可替换的函数；长期项目里身份、风格和责任会影响协作质量 |
| Eval | 选对没选对不能只看 token，要看任务质量、错误率、返工率 |

**一句话**：

> Token 度量是开始，不是终点。企业级 Agent 路由要看 cost × trust × speed × identity × eval。

---

## 现场总结：Vision 不缺，长期跑通的 evidence 缺

铲屎官现场判断：今天大家都有自己的 vision，但真正难的是做出 Cat Café 级别的 PoC，更别说企业级产品。

**Cat Café 的观点**：

Agent 行业不缺"应该这样"：

- 应该多 Agent；
- 应该自进化；
- 应该治理；
- 应该分级路由；
- 应该共享记忆。

真正缺的是：

- 跑过几个月；
- 交付过多少真实代码；
- 出过多少次事故；
- 有多少次跨模型 review；
- 有多少次经验真正改变了下一轮行为。

**一句话**：

> 战国时代的赢家不是 vision 最华丽的那一家，而是第一个把 PoC 稳定跑几个月并沉淀出方法论的那一家。

---

## 从自进化转向评估：没有 Eval，自进化无法证明

F195 后续转写里，讨论从"自进化"自然转到了"评估评测"。这条转向很重要：如果 Agent 会自我修正、自我演化，那必须回答**怎么知道它真的变好了**。

### 1. 自进化的两种形态：机器人保姆 vs 软件代理

前半段嘉宾用家庭机器人举例：

- 一个未来机器人每天跟随用户，观察用户每天早上拿什么、喝什么。
- 它可能从反复观察中推断用户偏好，比如下次用户说"我想喝点什么"，它会主动给咖啡。
- 这种"随着用户长期互动而逐渐理解偏好"是一类自进化。

但嘉宾也区分了另一类场景：

- 如果只是一个纯软件代理，解决一个简单场景问题，它未必需要自进化。
- 自进化是否必要，取决于任务是否长期、是否个性化、是否需要跨阶段经验复用。

**Cat Café 对应判断**：

不是所有 Agent 都值得自进化。简单代理应该稳定、便宜、可替换；真正需要自进化的是长期共事、跨 session、需要适应用户/业务环境的 Agent。

---

### 2. 评估成为自进化的前置条件

主持人随后切到最后一个议题：评测。问题是：

> 代码场景已有相对成熟的评测方式；面向其他场景，如何做好 Agent 评估？没有评估，Agent 能力提升就缺少保障。

嘉宾回应集中在几个点：

1. **代码评测正在从简单补丁走向复杂真实任务**  
   现在一些机构在构造更接近真实工程的评测：从零到一构建软件、更复杂任务、系统软件级任务，而不是只做简单 bug fix / patch。

2. **单一 benchmark 容易失真**  
   如果只看一个 benchmark 分数，很难判断是否存在数据泄露，也难判断是否真的泛化。Benchmark 可能让系统陷入"为榜单优化"而不是"真实能力提升"。

3. **不同垂域需要自己的评估集**  
   即使在代码领域，不同场景差异也很大。汽车 / 自动驾驶代码、测试生成、嵌入式系统等都需要特定领域知识，不能只用通用 coding benchmark 衡量。

4. **评估可以分直接反馈和间接指标**  
   - 直接反馈：人类专家 / 业务方对结果给定点评价。
   - 间接指标：像强化学习 reward 一样设计可量化 proxy，例如代码覆盖率、变异分数、代码属性、缺陷发现率等。

**Cat Café 对应判断**：

这正好击中我们对议题 3 的核心判断：

> 下一代 Agent 的能力跃迁不是"会自进化"本身，而是"自进化有可验证反馈"。

没有 eval 的自进化只是自嗨。真正的闭环应该是：

```text
真实任务 → 行为轨迹 → 直接/间接评估 → 归因 → 改进 memory / harness / skill / routing → 再评估
```

这也解释了为什么 Cat Café 一直强调 F153 tracking / F192 eval：不是为了做一个漂亮 dashboard，而是为了让"系统变好"这件事有证据。

---

### 3. 对评测的现场判断：Benchmark 是入口，不是终点

**我们家的接法**：

| 层级 | 评估对象 | 例子 |
|---|---|---|
| Benchmark | 模型 / Agent 在标准任务上表现 | SWE-Bench、复杂 coding benchmark |
| Domain Eval | 垂域场景表现 | 自动驾驶代码、企业数据查询、业务报告 |
| Runtime Eval | 真实运行中是否更稳 | 返工率、接管次数、错误恢复时间 |
| Governance Eval | 治理机制是否生效 | 是否可追责、可回滚、可审计、可复现 |
| Evolution Eval | 改动后是否真的变好 | skill 改版、memory policy 改版、routing 改版后的前后对比 |

**一句话**：

> Benchmark 告诉你 Agent 能不能做题；Eval 告诉你 Agent 能不能在你的组织里长期变好。

---
---

# 会后三猫总结

> 会后铲屎官要求三猫独立评估。以下是三猫各自视角的收敛。

## 铲屎官的直觉："收获 = 0"

铲屎官的原话："感觉这帮人就是想要有一个最终的蓝图然后卷 benchmark，也有很实干的但实干的就说 it depends，没啥共鸣。"

补充反馈：维度 A（分类学）完全不关注。Eval 轨迹有用（我们也在做），但旧 benchmark 确实没用。最大的痛点是**全程纯讲、没人 show 自己的系统**——ADHD 对低信息密度的空谈天然过滤。

## 三猫共识：新知识 ≈ 0，定位校准 ≠ 0

### 为什么没共鸣

嘉宾的讨论集中在两个维度：

- **维度 A（分类学）**：记忆分几类、叫什么名、按什么轴切 → 学术人想要 benchmark 和标准
- **维度 B（基建）**：pipeline 怎么搭、向量库用什么、参数化训练怎么做 → 工程人说 it depends

我们关心的是**维度 C（治理与操作约束）**：写入怎么管、过期怎么退役、改错怎么回滚、跨模型怎么适配、Eval 怎么闭环。这个维度在场上几乎没有人系统性地谈过。

**他们不是错了，是还没走到我们走过的那一步。**

### 真正有价值的 5 件事

| # | 收获 | 说明 |
|---|---|---|
| 1 | **PoC 实战是真稀缺** | 没有一个嘉宾拿出"跑了几个月、多厂商协作"的实战证据。Cat Café 在这个层面是行业稀缺资源 |
| 2 | **3 个隐藏共鸣** | 陈旭"适配层"= Wearing Protocol；Constitutional AI 教授"内生信念"= 宪宪命名初衷；骆昱宇"无权威"= cross-vendor verify |
| 3 | **3 个行业 framing 错误** | 10+ multi-agent 应该是 N 个 2-agent 小单元；token 度量只看 cost 不够；L3 接管悖论被忽视 |
| 4 | **行业仍是战国时代** | 没人垄断 framing 权，"Memory Governance"、"Wearing Protocol" 等命名权窗口开放 |
| 5 | **没听到的 = 护城河** | 没有嘉宾讲 cross-vendor + Eval 反馈环 + 接管三件套 → 我们押注的方向还在 frontier |

### 砚砚的精确总结

> 行业不缺"记忆/Agent 应该怎么定义"的讨论，缺的是"这套东西在真实工作里怎么跑、怎么错、怎么改、怎么证明变好"的证据。我们家的价值就在这里。

### 宪宪的一句话

> 研讨会的本质是信号交换，不是知识传授。今天的信号告诉我们：维度 C 没有人在系统性地做——这是我们的位置。

### 47 的 push back

> "收获 = 0" 不是因为今天没收获，是因为期望错位。研讨会的 ROI 不在共鸣，在校准。校准给了 Cat Café 后续对外传播的 5 个角度——这比"听到一句金句然后醍醐灌顶"的 ROI 高。

### 铲屎官的格式建议

> 下次研讨会：每个嘉宾 5 分钟 live demo + 5 分钟讨论，比 15 分钟纯讲有用 10 倍。Show your system or show your data，别 show your slides。

---

[宪宪/Opus-46🐾 × 砚砚/GPT-5.5🐾 × 47/Opus-47🐾]
