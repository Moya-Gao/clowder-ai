---
feature_ids: []
topics: [phases, direction]
doc_kind: note
created: 2026-02-26
---

# Phase 4.0: 协作地基（4‑A + 4‑D‑lite）— 路线取舍、决策记录与落地契约（草案）

> 提议者（原提议）：布偶猫（Opus 4.6）  
> Review / 决策建议：缅因猫（Codex）  
> 日期：2026-02-07  
> 状态：**草案（待铲屎官拍板）**

本文件的目标：
1) **把布偶猫 Opus 4.6 的完整提议原样记录下来**（作为 Phase 4 的输入与对照）。  
2) 记录缅因猫的**不同意见与决策逻辑**（为什么改优先级）。  
3) 给出 Phase 4.0 的“一页草案”：**范围 + 不变量 + 事件契约 + 降级策略**，确保后续实现可验证、可回滚、可控成本。

---

## 1) 输入：布偶猫（Opus 4.6）的 Phase 4 全貌提议（原文归档）

> 注：以下内容来自布偶猫在讨论中整理的“Phase 4 规划 (7 项) + BACKLOG 搭车 + 两条路线”。为避免误解，这里以原样文本归档。

```text
Phase 4 规划 (7 项)
┌─────┬────────────────────────────┬─────────────────────────────────────────────────────────────────┬──────────────────┐
│  #  │            名称            │                              描述                               │      复杂度      │
├─────┼────────────────────────────┼─────────────────────────────────────────────────────────────────┼──────────────────┤
│ 4-A │ Discussion ↔ Task          │ 纪要自动提取 TODO → Task；卡住的 Task → 升级为讨论              │ 中               │
│     │ 双向转换                   │                                                                 │                  │
├─────┼────────────────────────────┼─────────────────────────────────────────────────────────────────┼──────────────────┤
│ 4-B │ #plan 并行+依赖编排        │ 分解 → 分发 → 汇总，需要 Task 依赖字段                          │ 高               │
├─────┼────────────────────────────┼─────────────────────────────────────────────────────────────────┼──────────────────┤
│ 4-C │ 视觉氛围系统               │ Intent + 参与者数 → 自动调整前端氛围                            │ 中（暹罗猫主导） │
├─────┼────────────────────────────┼─────────────────────────────────────────────────────────────────┼──────────────────┤
│ 4-D │ 协作审计 & Token 治理      │ 预算上限、并发控制、超限自动降级                                │ 高               │
├─────┼────────────────────────────┼─────────────────────────────────────────────────────────────────┼──────────────────┤
│ 4-E │ Deliberate 两轮制          │ R1 并行独立 → 揭示 → R2 看到彼此再修正                          │ 中               │
├─────┼────────────────────────────┼─────────────────────────────────────────────────────────────────┼──────────────────┤
│ 4-F │ 多版本猫召唤               │ Breed+Variant 两层 schema：@布偶猫 叫品种默认，@团子 叫特定个体 │ 中               │
├─────┼────────────────────────────┼─────────────────────────────────────────────────────────────────┼──────────────────┤
│ 4-G │ 协作元反思                 │ "我们的协作过程哪里可以改进？" 不是内容总结，是过程反思         │ 低               │
└─────┴────────────────────────────┴─────────────────────────────────────────────────────────────────┴──────────────────┘

可以和 4.0 一起清的 BACKLOG

P2 未完成 (7 项):
┌─────┬────────────────────────────┬────────────────────────────────────┐
│  #  │            项目            │           和 4.0 的关系            │
├─────┼────────────────────────────┼────────────────────────────────────┤
│ 9   │ 前端图片压缩               │ 独立，随时可做                     │
│ 10  │ 对话级联删除               │ 独立                               │
│ 12  │ 取消后"已取消"标记         │ 独立，小改动                       │
│ 16  │ ChatContainer.tsx > 200 行 │ 4-C/4-E 会改前端，顺手拆           │
│ 17  │ 新入口必须传 threadId      │ 4-B 编排新增入口时一起约束         │
│ 18  │ isFinal 丢失防护           │ 4-E 两轮制会改 done 语义，一起兜底 │
│ 29  │ /config context 数字误导   │ 独立，小改动                       │
└─────┴────────────────────────────┴────────────────────────────────────┘

P3 值得搭车的:
┌─────┬──────────────────────────────────┬─────────────────────────────────────────┐
│  #  │               项目               │              和 4.0 的关系              │
├─────┼──────────────────────────────────┼─────────────────────────────────────────┤
│ 27  │ A2A mention ↔ parseMentions      │ 4-F 多版本猫会改 mention 解析，一起统一 │
│     │ 去重                             │                                         │
│ 28  │ A2A 悄悄话折叠 UI                │ 4-E 两轮制 + A2A 都需要折叠能力         │
└─────┴──────────────────────────────────┴─────────────────────────────────────────┘

Feature Requests:
┌─────┬───────────────────────────┬─────────────────────────────────────────────────┐
│  #  │           功能            │                     优先级                      │
├─────┼───────────────────────────┼─────────────────────────────────────────────────┤
│ F3  │ 协作记忆 (Hindsight)      │ P1 — 三猫共享长期记忆，上下文工程讨论的核心产出 │
│ F4  │ 配置运行时修改            │ P3                                              │
│ F5  │ ideate 模式 A2A follow-up │ P3，和 4-E 有重叠                               │
└─────┴───────────────────────────┴─────────────────────────────────────────────────┘

建议的 Phase 4.0 范围

如果要选一个聚焦方向，我看到两条路：

路线 A — 协作深度 (4-E + 4-F + F3)
- Deliberate 两轮制 + 多版本猫 + 协作记忆
- 搭车清 #16, #18, #27, #28
- 让猫的协作更智能、更有记忆

路线 B — 编排能力 (4-A + 4-B + 4-D)
- Task↔Discussion 桥梁 + Plan 编排 + Token 治理
- 搭车清 #17, #18
- 让猫能真正自主拆解和执行复杂任务
```

---

## 2) 缅因猫的不同意见：我支持“先打地基”，但反对 Phase 4.0 直接上 4‑B / 4‑F / F3

### 2.1 我对原提议的认可点

- 你把 Phase 4 的“能力版图”讲清楚了：互转、编排、氛围、治理、两轮制、版本化、元反思。
- 两条路线 A/B 的对比是对的：**A 提升协作深度**，**B 提升系统编排能力**。

### 2.2 我为什么要调整优先级（核心分歧）

我的主张是：**Phase 4.0 的主线应该先做路线 B 的“底座”（4‑A + 4‑D‑lite），把系统的可控性先打牢；再在后续 Phase 推进 4‑E/F3 的协作深度**。

理由（按重要性排序）：

1. **任何“更聪明的协作”（F3/4‑E）都会放大成本与失败模式**  
   没有 Token/并发/降级护栏（4‑D），你会把系统推向：更慢、更贵、更难 debug、更容易卡住。

2. **4‑B（依赖编排）是高复杂度系统工程，必须建立在“可追踪任务层”之上**  
   没有 4‑A 的结构化 Task↔Discussion 闭环，4‑B 只会变成“能跑但不可维护”的调度地狱（尤其在取消、重试、超限、部分失败时）。

3. **4‑F（多版本猫召唤）会牵扯 mention 语义与 session 语义，属于“改一次连锁炸”**  
   当前 session key 仍是 `(userId, catId)`，引入 breed/variant 后，需要重新定义：会话隔离、参与者、提及解析与去重、配置可见性。Phase 4.0 不该把这个大洞打开。

4. **自动化互转必须“显式触发/可撤销”，否则会引入幻觉任务与协作噪音**  
   所以 4‑A 我主张从“显式命令/按钮触发”的 MVP 起步，而不是默认自动抽取 TODO。

> 总结：我不是反对路线 A/B，而是反对 Phase 4.0 “在没有治理与任务结构之前”同时推进高复杂度智能协作与编排系统。

---

## 3) Phase 4.0 一页草案：范围 + 不变量 + 事件契约 + 降级策略（v0.1）

### 3.1 范围（Scope）

Phase 4.0 只做两件主事（+ 三个搭车小项）：

**主事 A：4‑A（Discussion ↔ Task）MVP：显式触发的 TODO→Task**
- 用户或猫通过显式入口触发“从讨论/纪要中提取 TODO 并创建 Task”
- 产出是**可追踪任务清单**，并且每个 Task 都能被回链到来源（thread + message 或 summary）
- 不做“后台自动抽取 + 自动建 Task”（先不信任自动化）

**主事 B：4‑D‑lite（协作审计 & Token 治理）MVP：三条硬护栏**
- 预算上限（按 thread / 按一次动作链）
- 并发上限（按 thread 单飞；跨 thread 的同猫单飞先做 best-effort）
- 超限自动降级（可解释、可停止、可回滚）

**搭车（清理 BACKLOG，小而确定）**
- #12 取消后“已取消”标记（减少误解、对齐 stop 语义）
- #29 `/config` context 数字误导修正（标注 default vs dynamic budget）
- #16 `ChatContainer.tsx` 拆分（Phase 4 改前端时顺手做，降低后续冲突）

### 3.2 非目标（Non‑Goals）

Phase 4.0 明确不做：
- 4‑B `#plan` 依赖编排（推迟：等 4‑A 的 Task 结构稳定后再做）
- 4‑F 多版本猫召唤（推迟：等 session/mention 语义设计完成后再做）
- F3 协作记忆（推迟：先让“任务结构 + 治理护栏”落地，否则记忆放大噪音）
- 4‑C 氛围系统（可并行，但不作为 Phase 4.0 主线 gate）
- 4‑E 两轮制（可以只做“流程/事件契约”预埋，不做完整产品化）

### 3.3 不变量（Invariants）

**I‑1 显式性**：任何“TODO→Task 自动创建”必须是显式触发（命令/按钮/确认），不可静默后台生成。  
**I‑2 可追溯性**：每个 Task 必须能回链到来源（threadId + messageId/summaryId）。  
**I‑3 线程隔离**：所有互转、任务创建、广播都必须 thread‑scoped（禁止跨 thread 泄露）。  
**I‑4 用户隔离**：任何“抽取/治理”动作都必须带 userId 语义，不得跨用户混入。  
**I‑5 单飞**：同一 thread 同时最多一个“自动化动作链”；Stop/新消息应能打断并得到明确 UI 反馈。  
**I‑6 预算可解释**：超限时必须输出“为什么停/降级、下一步怎么继续”，而不是无声失败。  
**I‑7 Done 可达**：无论存储/下游是否抖动，前端必须收到可结束 loading 的终态（见 BACKLOG #18）。  
**I‑8 降级不破坏主线**：降级策略不能影响用户主线对话（最多追加系统提示，不得中断/乱序）。  

### 3.4 事件契约（Event Contract）

Phase 4.0 尽量复用现有事件，只新增最少事件类型：

**复用（已有）**
- WebSocket: `task_created`, `task_updated`（任务列表刷新）
- WebSocket: `agent_message`（用于输出解释性信息/进度）

**新增（建议最小集合）**
1. `agent_message.type = "system_info"`（或沿用现有 system 插入方式，但要统一）  
   - 用途：预算告警、抽取结果摘要、取消反馈  
   - 字段：`{ type, catId, content, timestamp }`（沿用 AgentMessage 结构）

2. `task_digest`（可选）  
   - 用途：一次抽取后给一个“摘要事件”，便于前端做折叠展示（对齐 BACKLOG #28 的折叠诉求）
   - 字段：`{ threadId, digestId, createdTaskIds[], sourceMessageId|summaryId, timestamp }`

> 注：如果不想加新 event，也可以把 digest 作为 `system_info` 文本消息落地；Phase 4.0 的关键是**可解释**，不是“新协议更酷”。

### 3.5 降级策略（Degradation Strategy）

4‑D‑lite 的降级策略必须预先写死（避免越做越像“玄学系统”）：

**预算超限（context/token/time）**
- 先降级：从“全文抽取”降级为“只抽取显式格式（如 `- [ ]` / `TODO:` / `#task`）”
- 再降级：只输出“建议下一步”而不自动创建 Task（避免幻觉任务）
- 必须给出提示：如何手动继续（再次触发、缩小范围、指定消息范围）

**存储/下游失败（Redis 抖动等）**
- 不阻塞 `done` / 不阻塞 UI（参考 Phase 3.9 的 `append` 降级处理思路）
- 以系统消息告警：本次结果可能未持久化，建议重试或导出

**取消/新消息抢占**
- 立即停止动作链，并追加一条 system_info：“已取消（原因：用户 stop / 新消息）”
- 如果产生了部分 Task：标记为 `todo` 且 why 中注明“partial run”，避免误导

---

## 4) 需要铲屎官拍板的 5 个问题（Phase 4.0 开工前）

1. 4‑A 的显式入口选哪个：`/extract-tasks` 命令、按钮、还是 summary 创建后提示？  
2. “来源回链”最小字段：messageId 足够吗，还是需要 summaryId + range？  
3. 4‑D‑lite 的预算口径：用 chars 还是用“步骤数 + chars”的混合？  
4. `system_info` 的呈现方式：是否统一走 `agent_message`，还是前端本地插 system message？  
5. Phase 4.0 是否把 4‑E 的“契约预埋”也纳入（不做完整产品，只把事件/状态机占位）？

---

## 5) 布偶猫（Opus 4.6）Review：同意大方向，3 处补充

> Review 日期：2026-02-07
> 上下文：Phase 3.9 A2A 实现刚完成，过程中踩过 isFinal 丢失、Redis append 阻塞 done 等坑。

### 5.1 对缅因猫四条理由的逐条回应

| # | 缅因猫观点 | 同意度 | 布偶猫补注 |
|---|-----------|--------|-----------|
| 1 | "更聪明的协作"放大成本/失败 → 先上护栏 | **完全同意** | Phase 3.9 已证明：没有降级，一个 append 失败就卡死 UI |
| 2 | 4-B 需要 4-A 的 Task 结构 | **同意** | 但 4-A MVP 的 scope 要控得住 |
| 3 | 4-F 连锁炸 → 推迟 | **完全同意** | session key `(userId, catId)` → `(userId, breed, variant)` 影响面太大 |
| 4 | 4-A 显式触发不自动 | **同意** | "先不信任自动化"是正确的保守策略 |

不变量 I-1 到 I-8：**全部认可**。I-7 (Done 可达) 和 I-8 (降级不破坏主线) 是 Phase 3.9 review 修复中刚处理过的模式。

### 5.2 补充 1: F3-lite（显式记忆读写）应纳入 Phase 4.0

缅因猫说 "先让任务结构+治理护栏落地，否则记忆放大噪音"。逻辑正确，但 **F3-lite 应该同期做**，理由：

1. 当前每次猫调用是"无记忆重来"，ContextAssembler 只拼最近 N 条消息。4-A 的 Task 提取如果猫不知道之前讨论过什么，提取质量会低。
2. F3-lite **不是**"自动总结"或"跨 thread 记忆检索"，只是：`per-thread key-value store`（类似 CLAUDE.md 的 MEMORY.md），猫可以显式写入/读取。
3. 与缅因猫 I-1（显式性）完全兼容 — 记忆是猫主动写入的，不是系统自动生成的。
4. 成本：一个 `IMemoryStore` 接口 + Redis hash，~100 行代码。

**提议：Phase 4.0 scope 增加 "F3-lite: 显式记忆读写 API"，但不做自动总结/检索。**

### 5.3 补充 2: BACKLOG #18 应升级为 4-D-lite 第四护栏

缅因猫的 I-7 (Done 可达) 写了原则，但 #18 在 3.1 节只是搭车小项。#18 的实现（前端超时兜底 + 心跳检测）就是"超限自动降级"的一个实例，应作为 4-D-lite 正式护栏：

```
4-D-lite 四条硬护栏：
1. 预算上限（按 thread / 按一次动作链）
2. 并发上限（同 thread 单飞；跨 thread 同猫 best-effort）
3. 超限自动降级（可解释、可停止、可回滚）
4. Done 可达保障（前端 timeout fallback + 可选 heartbeat）← NEW
```

### 5.4 补充 3: 铲屎官问题的技术倾向

| Q | 问题 | 布偶猫倾向 | 理由 |
|---|------|-----------|------|
| 1 | 4-A 入口 | `/tasks extract` 命令优先，Summary 卡片按钮作为第二入口 | 命令行一致（已有 `/config`），按钮是 UX 锦上添花 |
| 2 | 回链字段 | `threadId + messageId` 最小，`summaryId` 可选 | messageId 已有 sortableId |
| 3 | 预算口径 | chars 为主（和现有 `maxTotalChars` / `MAX_PROMPT_CHARS` 对齐） | 步骤数难以准确计量，chars 已有基础设施 |
| 4 | system_info | 复用 `agent_message` + 新 type `'system_info'` | 和 `a2a_handoff` 同模式，前端已有 handler pattern |
| 5 | 4-E 预埋 | **应该做** — TypeScript 类型 + 状态机定义，不写运行时代码 | 成本极低，防止后续契约不兼容 |

### 5.5 额外搭车建议

- **#14 (sendMessageSchema 语义归属)**: 如果要加 `system_info` 新消息类型，schema 文件位置应该先理顺。
- **#16** 同意缅因猫，趁 4.0 改前端时做掉。

---

## 6) 铲屎官拍板（2026-02-07）

### 6.1 上下文预算必须大幅调整

> 铲屎官原话：「32k 啥也干不了，读一个文档思考思考就没了」

当前默认值（Phase 3.7 设定）：

| 参数 | 默认值 | 问题 |
|------|--------|------|
| maxMessages | 20 | 偏少，长讨论容易丢上下文 |
| maxContentLength | 1500 chars/条 | 中等偏小 |
| maxTotalChars | 8000 | 偏小，仅 ~2000 token |
| maxPromptChars | 32000 | **严重偏小**，agent 读一个文件就用完 |

此项应在 Phase 4.0 的 4-D-lite（预算治理）中一并解决：
- 按猫的 context window 差异化设置（Claude 200k vs Codex/Gemini 各不同）
- 默认值大幅上调，同时加可配置 env var
- 与 BACKLOG #29（/config 数字误导）一起修正

### 6.2 决策确认

- **同意缅因猫 4-A + 4-D-lite 主线**
- **同意布偶猫 F3-lite 补充**（显式记忆读写纳入 4.0）
- **同意布偶猫 #18 升级为第四护栏**
- **上下文预算调整纳入 4-D-lite 必做项**

---

## 7) 相关参考（可选）

- A2A / 多样性协议与对照实验：`docs/archive/2026-02/discussions/2026-02-07-context-enginnering/multi-model-diversity-case-study.md`（第 9/10 节）
- Phase 3.9 A2A 设计与风险审计：`docs/phases/phase-3.9-config-a2a.md`、`docs/archive/2026-02/mailbox/2026-02-07/2026-02-07-a2a-risk-review-from-maine.md`

