# F193 Phase C — Split-only 配置 + limb 迁移 Implementation Plan

**Feature:** F193 — `docs/features/F193-cross-thread-comm-unification.md`
**Goal:** 砍 all-in-one `cat-cafe` server，按 F043 立项原意改 split-only；新增 `cat-cafe-limb` server 承接现有 limb tools，避免 split-only 让 limb 静默掉线。
**Acceptance Criteria（从 spec 抄过来）:**
- AC-C1: 新增 `cat-cafe-limb` server entry point（`packages/mcp-server/src/limb.ts` + `dist/limb.js` build artifact）
- AC-C2: `.mcp.json` 删 `cat-cafe` entry，加 `cat-cafe-limb`
- AC-C3: `.codex/config.toml` 删 `[mcp_servers.cat-cafe]`，加 `[mcp_servers.cat-cafe-limb]`
- AC-C4: `tool-registration.test.js` 守护 split-only 模式 + limb 工具仅在 `cat-cafe-limb` server 可见

**Architecture cell:** `transport`（cross_post_message + cell tools edge）+ 所有 cell 都被 split server 拆分影响（间接）
**Map delta:** none（Phase A 已加 transport cell cited_by F193）— Phase C 是配置 layer，不改 cell ownership 边界
**Map delta why:** server 拓扑变化是配置 plumbing，不重新画 ownership 图。`registerLimbToolset` Phase A 之前就存在（[server-toolsets.ts:119](../../packages/mcp-server/src/server-toolsets.ts)），Phase C 只是把它**单独包装成 entry point** + 配置切换。
**Architecture:** Mirror `collab.ts` / `memory.ts` 的 split server 套路 — 新建 `limb.ts` entry point 调 `createLimbServer()` 工厂，`registerLimbToolset(server)`，stdio transport。然后两个 harness 配置（`.mcp.json` + `.codex/config.toml`）从 4 server (full + 3 split) 切到 4 split (collab + memory + signals + limb)。
**Tech Stack:** TypeScript / `@modelcontextprotocol/sdk` / node:test
**前端验证:** No — 纯 MCP server 配置 + entry point 改动

---

## Straight-Line Check

**B (finish line)**: 4 个 split server 在两个 harness 配置里都列齐（collab / memory / signals / limb），all-in-one `cat-cafe` server 从配置和测试守护中**完全消失**。`registerFullToolset` 在 `index.ts` 仍保留（向后兼容 / 测试用）但不再被默认 harness 使用。猫调用 `limb_*` 工具时通过 `cat-cafe-limb` namespace 路由。

**Not building**:
- 删除 `index.ts` 全量 server（不在 spec scope —— 留作 fallback / 向后兼容）
- 改 `registerFullToolset` 内部行为
- 任何 limb tool 自身的实现改动（限于注册迁移）

**Terminal schema**:

```typescript
// packages/mcp-server/src/limb.ts (NEW — mirror collab.ts)
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';
import { registerLimbToolset } from './server-toolsets.js';
import { initCatCafeDir } from './utils/path-validator.js';

export function createLimbServer(): McpServer {
  const server = new McpServer({ name: 'cat-cafe-limb-mcp', version: '0.1.0' });
  registerLimbToolset(server);
  return server;
}
// + main() + entry-point check (mirror collab.ts:32-50)
```

```jsonc
// .mcp.json terminal — 4 split servers, no all-in-one
{
  "mcpServers": {
    "cat-cafe-collab": { "command": "node", "args": ["packages/mcp-server/dist/collab.js"] },
    "cat-cafe-memory": { "command": "node", "args": ["packages/mcp-server/dist/memory.js"] },
    "cat-cafe-signals": { "command": "node", "args": ["packages/mcp-server/dist/signals.js"] },
    "cat-cafe-limb":   { "command": "node", "args": ["packages/mcp-server/dist/limb.js"] }
    // [cat-cafe] (all-in-one) entry REMOVED
  }
}
```

**每步通过三问**：每个 step 产物都是终态—— `limb.ts` 一旦写，永远是 limb namespace 的 entry；配置改动一旦切，`cat-cafe` 不再被默认 harness 加载。无 throwaway scaffolding。

---

## Task 1: AC-C1 — 新增 `limb.ts` entry point

**Files:**
- Create: `packages/mcp-server/src/limb.ts`
- Verify: `packages/mcp-server/dist/limb.js` (auto-built by `tsc`)

**Step 1.1: 实现 limb.ts**

直接 mirror `collab.ts` 模式（已在 codebase 里）:

```typescript
#!/usr/bin/env node
/** Cat Café MCP Server — Limb Surface (布偶猫专属能力 namespace). */
import { resolve } from 'node:path';
import { fileURLToPath } from 'node:url';
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';
import { registerLimbToolset } from './server-toolsets.js';
import { initCatCafeDir } from './utils/path-validator.js';

export function createLimbServer(): McpServer {
  const server = new McpServer({ name: 'cat-cafe-limb-mcp', version: '0.1.0' });
  registerLimbToolset(server);
  return server;
}

async function main(): Promise<void> {
  initCatCafeDir();
  const server = createLimbServer();
  const transport = new StdioServerTransport();
  console.error('[cat-cafe-limb] MCP Server starting...');
  await server.connect(transport);
  console.error('[cat-cafe-limb] MCP Server running on stdio');
}

const isEntryPoint = process.argv[1] && resolve(fileURLToPath(import.meta.url)) === resolve(process.argv[1]);
if (isEntryPoint) {
  main().catch((err) => {
    console.error('[cat-cafe-limb] Fatal error:', err);
    process.exit(1);
  });
}
```

**Step 1.2: Build + verify**

```bash
pnpm --filter @cat-cafe/mcp-server build
ls packages/mcp-server/dist/limb.js   # must exist
node -e "import('./packages/mcp-server/dist/limb.js').then(m => console.log(typeof m.createLimbServer))"
# Expected: 'function'
```

---

## Task 2: AC-C4 — tool-registration.test.js 守护 createLimbServer

**Files:**
- Modify: `packages/mcp-server/test/tool-registration.test.js` — 加 `createLimbServer registers only limb tool surface` test (mirror existing collab/memory/signals test pattern)

**Step 2.1: Find existing pattern**

Tests at lines ~278+ already have:
- `createCollabServer registers only collab tool surface`
- `createMemoryServer registers only memory tool surface`
- `createSignalsServer registers only signals tool surface`

Add 4th: `createLimbServer registers only limb tool surface` — assert only `limb_*` tools registered, no collab/memory/signals tools.

**Step 2.2: Implementation**

Mirror existing pattern (create server → assert tool name set matches expected limb tool list).

**Step 2.3: Run test**

```bash
pnpm --filter @cat-cafe/mcp-server test
# 172/172 baseline + 1 new = 173/173
```

---

## Task 3: AC-C2 + AC-C3 — Harness 配置切换

**Files:**
- Modify: `.mcp.json` — 删 `cat-cafe` entry，加 `cat-cafe-limb`
- Modify: `.codex/config.toml` — 删 `[mcp_servers.cat-cafe]` block，加 `[mcp_servers.cat-cafe-limb]` block

**Step 3.1: `.mcp.json` 改动**

```diff
 "mcpServers": {
-  "cat-cafe": { "command": "node", "args": ["packages/mcp-server/dist/index.js"] },
   "cat-cafe-collab": { ... },
   "cat-cafe-memory": { ... },
-  "cat-cafe-signals": { ... }
+  "cat-cafe-signals": { ... },
+  "cat-cafe-limb": { "command": "node", "args": ["packages/mcp-server/dist/limb.js"] }
 }
```

**Step 3.2: `.codex/config.toml` 改动**

```diff
-[mcp_servers.cat-cafe]
-command = "node"
-args = [ "packages/mcp-server/dist/index.js" ]
-enabled = true
-
 [mcp_servers.cat-cafe-signals]
 ...
+
+[mcp_servers.cat-cafe-limb]
+command = "node"
+args = [ "/Users/lysander/projects/relay-station/cat-cafe/packages/mcp-server/dist/limb.js" ]
+enabled = true
```

**Step 3.3: Verify config syntax**

`jq '.' .mcp.json` 应解析无误；`.codex/config.toml` toml 语法 OK。

---

## Task 4: AC-C4 (full) — split-only 守护测试

**Files:**
- Modify: `packages/mcp-server/test/tool-registration.test.js`
- Optional: `scripts/check-mcp-config-split-only.mjs` (新 lint，扫两个 harness 配置确保 `cat-cafe` 不在)

**Step 4.1: 写 split-only 守护测试**

加 test：scan 项目根目录的 `.mcp.json` + `.codex/config.toml`，断言 4 个 split server (`cat-cafe-collab` / `cat-cafe-memory` / `cat-cafe-signals` / `cat-cafe-limb`) 都存在 + `cat-cafe` (全量入口) **不存在**。

**Step 4.2: Run + commit**

```bash
pnpm --filter @cat-cafe/mcp-server test
# 173/173 baseline + 2 new守护 = 175/175
```

---

## Task 5: 收尾 — Spec checkboxes + Architecture cell sync (if any)

**Files:**
- Modify: `docs/features/F193-cross-thread-comm-unification.md` — Phase C 4 AC ✅ + Timeline +1

**Step 5.1: Sync spec**

Phase C AC-C1/C2/C3/C4 全部打勾，附 commit SHA。Timeline 加：

```
| 2026-05-08 | Phase C — split-only + limb migration (PR pending) |
```

---

## Phase C 收尾验证

- pnpm --filter @cat-cafe/mcp-server test → 175/175 ✅（含 createLimbServer + split-only 守护）
- pnpm gate → 全绿（注：Phase A 已 land 后 backlog-routes flaky 仍可能 timeout，按 CVO 拍板继续）
- spec Phase C 4/4 ✅
- 砚砚 review → cloud review → squash merge

预估时间：1-1.5h（mirror 模式简单 + 4 task）

---

## Risk during implementation

| Risk | Mitigation |
|------|------------|
| 切换后某些猫的 invocation context 仍引用 `cat-cafe` namespace | grep `cat-cafe` 在 src/docs/test 里的引用，确认都是 split server prefix；invocation context 用 `mcp__cat-cafe-collab__*` etc. tool name 不应含 all-in-one |
| Antigravity / 其他 wrapper 用 all-in-one 入口 | grep `dist/index.js` 在 .mcp.json / .codex/config.toml 之外的引用；如果有，单独评估迁移 |
| limb 工具在测试 fixture 里用 `cat-cafe` namespace | grep test files for `cat-cafe.limb` namespace；如有，同步改 |
| harness OFFSET 模板的 .env.local 没受影响 | OFFSET 不影响 server registration（OFFSET 改的是 Redis / API / Web 端口；MCP servers stdio transport，不绑端口）|
