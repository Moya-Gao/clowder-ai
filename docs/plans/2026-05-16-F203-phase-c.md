# F203 Phase C — Native System Prompt 接通 + 精确剥离重复 Implementation Plan

**Feature:** F203 — `docs/features/F203-native-system-prompt-l0.md`
**Goal:** L0 从 user-message-prepend（会被压缩）切到 native system role（`--system-prompt-file`，压缩免疫），并**精确剥离** user message 里已被 L0 覆盖的重复，**保留**所有 per-invocation 动态上下文。
**Acceptance Criteria（F203 spec Phase C）:**
- AC-C1: `ClaudeBgCarrierService` argv 加 `--system-prompt-file <渲染L0>`，单测 + e2e（bg + system-prompt-file + mcp-config）
- AC-C2: `CodexAgentService` argv 加 `-c developer_instructions=...`，并发安全（@codex/@gpt52/@spark 各自独立 argv，不污染 ~/.codex/config.toml）
- AC-C3: `effectivePrompt` 拼装删除 buildStaticIdentity **非 pack 部分** prepend（已进 L0），保留 pack + invocationContext
- AC-C4: F-BLOAT 测试：resume 时 system prompt 不重复累积
- AC-C5: runtime 重启 + 47/46/砚砚 各跑一轮 + 铲屎官 10 轮压缩对话验收（CVO directive 不灰度，`git revert` 回滚）
**Architecture cell:** harness/system-prompt-injection
**Map delta:** update required（注入链 user-message-prepend → native-system-role；ADR-030 §3 已记新流程）
**Map delta why:** 改变 system prompt 注入通道（buildStaticIdentity 非 pack 部分从 user message 移到 system role），是 harness/system-prompt-injection cell 的 owner-level 通道变更
**Architecture:** compile-system-prompt-l0.mjs（Phase B 已交付）渲染 per-cat L0 → 写临时文件 → spawn `--system-prompt-file`。invoke-single-cat 的 effectivePrompt 改造：剥离 buildStaticIdentity 非 pack 部分，保留 pack blocks + buildInvocationContext + prompt。
**Tech Stack:** Node spawn argv / ClaudeBgCarrierService / CodexAgentService / invoke-single-cat / writeL0File (Phase B)
**前端验证:** No（纯 invocation 链路后端 + runtime 行为验收）

---

## 🔴 精确保留/删除清单（铲屎官 directive 核心——防"干着忘记"）

> 真相源：`SystemPromptBuilder.ts` buildStaticIdentity (L489-600) + buildInvocationContext (L607-827+)，2026-05-16 精确核对。

### A. buildStaticIdentity（L489-600）逐项处置

| # | 字段 (代码位置) | L0 是否覆盖 | 处置 | 理由 |
|---|---------------|-----------|------|------|
| A1 | Identity 你是X由Y提供 (L500-506) | ✅ L0 §1 IDENTITY_BLOCK | **删** user message | L0 已覆盖 |
| A2 | 硬限制 restrictions (L512-514) | ✅ L0 §1 buildIdentityBlock restrictions | **删** | L0 已覆盖 |
| A3 | **Pack masksBlock** (L517-519) | ❌ L0 不含（pack 动态/外部项目） | **保留** user message | F129 pack per-invocation 动态，进静态 L0 会破 cache + 外部项目特定 |
| A4 | A2A 协作格式 ##协作+@格式 (L521-545) | ⚠️ 部分 L0 §4（@路由规则）+ §1（队友@handles） | **删**（验证后）| L0 §4 含 @ 路由格式 + §1 TEAMMATE_ROSTER 含可@队友。**Step 0 spike 必须语义对比验证无遗漏** |
| A5 | 队友名册 buildTeammateRoster (L548-551) | ✅ L0 §1 TEAMMATE_ROSTER | **删** | L0 已覆盖（Phase B compile 同源 buildTeammateRoster）|
| A6 | WORKFLOW_TRIGGERS per-breed (L554-557) | ✅ L0 §6 WORKFLOW_TRIGGERS | **删** | L0 已覆盖（Phase B KD-8 含 displayName fallback）|
| A7 | **Pack workflowsBlock** (L561-563) | ❌ L0 不含 | **保留** | 同 A3，pack 动态 |
| A8 | 铲屎官 reference ccName/ccHandles (L568-571) | ⚠️ L0 §4 CVO 三硬条件硬编码 @landy | **删**（修 gap 后）| **GAP**: L0 §4 硬编码 @landy，buildStaticIdentity 用 co-creator config mention patterns。Step 1 修：compile 注入 CVO handles 模板变量，对齐 config |
| A9 | GOVERNANCE_L0_DIGEST (L575) | ✅ L0 §3 家规（digest 扩展版）| **删** | L0 §3 是 digest 的 14 项扩展，核心覆盖 |
| A10 | **Pack guardrail/defaults/worldDriver** (L578-590) | ❌ L0 不含 | **保留** | 同 A3，pack 动态 |
| A11 | MCP_TOOLS_SECTION (L595-597) | ✅ L0 §7 MCP quick index | **删** | L0 §7 已覆盖（ADR §10.2 KD-7 quick index 替代全量）|

**A 段结论**：删 A1/A2/A4/A5/A6/A8/A9/A11（非 pack，L0 覆盖）；**保留 A3/A7/A10（Pack blocks——pack 动态不进静态 L0）**。

### B. buildInvocationContext（L607-827+）逐项处置 — **全部保留 user message**

| # | 字段 | 处置 | 理由（per-invocation 动态，L0 静态不含）|
|---|------|------|------|
| B1 | Identity constant (L620-623) | **保留** | per-invocation pin（runtimeModel 每次 resolve）|
| B2 | directMessageFrom + 同族 anti-spoofing (L629-648) | **保留** | A2A 动态（谁发给我）|
| B3 | crossThreadReplyHint (L655-662) | **保留** | 跨线程动态（sourceThreadId 每次不同）|
| B4 | pingPongWarning (L666-672) | **保留** | 乒乓 streak 动态 |
| B5 | teammates 本次参与 (L675-684) | **保留** | 本次 invocation 参与猫（≠ L0 §1 全 roster）|
| B6 | mode serial/parallel/independent (L686-697) | **保留** | 每次模式不同 |
| B7 | A2A 球权检查 a2aEnabled (L701-706) | **保留** | 动态触发（依 a2aEnabled）；L0 §4 是静态规则，此为本次触发提醒——非重复（规则 vs 本次激活）|
| B8 | mentionRoutingFeedback (L709-715) | **保留** | one-shot 动态反馈 |
| B9 | promptTags critique/skill (L718-726) | **保留** | 本次 prompt tag 动态 |
| B10 | activeParticipants 最近活跃 (L729-739) | **保留** | thread 活跃状态动态 |
| B11 | routingPolicy (L742-773) | **保留** | thread routing policy 动态 |
| B12 | sopStageHint (L776-780) | **保留** | SOP stage 动态（告示牌）|
| B13 | voiceMode (L783-794) | **保留** | 语音模式动态 |
| B14 | bootcampState (L797-806) | **保留** | bootcamp phase 动态 |
| B15 | guideCandidate (L809-811) | **保留** | guide 匹配动态 |
| B16 | worldContext (L814-827+) | **保留** | 世界观模式动态 |

**B 段结论**：**全保留**——buildInvocationContext 100% per-invocation 动态，L0 是 per-cat 静态（压缩免疫 + cache 稳定要求静态），动态绝不能进 L0。

### 一句话总结（压缩后自读用）

> **删**：buildStaticIdentity 非 pack 8 项（A1/2/4/5/6/8/9/11，L0 已覆盖）。
> **留**：buildStaticIdentity 的 3 个 Pack block（A3/7/10）+ buildInvocationContext 全 16 项（B1-16，per-invocation 动态）。
> 删错 B 段任一 = 猫丢球权/SOP/跨线程上下文 = 协作链崩。

---

## Straight-Line Check

- **B 定义**：所有 Claude/Codex 猫 invocation 的 L0（14 项家规 + 客观性 placeholder）走 system role（压缩免疫），user message 只剩 pack blocks + invocationContext + prompt。
- **终态 schema**：`effectivePrompt = invocationContext + (packBlocks if any) + prompt`（**不再** prepend buildStaticIdentity 非 pack 部分）；L0 经 `--system-prompt-file` 进 system role。
- **NOT building**：不灰度（CVO directive）、不留 feature flag、不动 buildInvocationContext 任何字段、不重写客观性（placeholder，CVO 已拍板）。

---

## Tasks

> 🔴 **执行顺序（与编号不同——防回归窗口）**：Task 0 → Task 1 → **Task 3a（共享 helper）→ Task 3 + Task 4（接通 L0 进 system role，additive）→ Task 2（删 user message 重复）** → Task 5。
> 编号按 AC 排，**执行按依赖排**：先接通后删重复（CVO directive「接通之后再删重复」）。Task 2 单独先行 = 有窗口非 pack 既不在 system role 也不在 user message。Task 0/1 已完成（commit `ca3efead7` / `fd4e634ca`）。

### Task 0: 语义对比 spike（去重前置——验证 L0 真覆盖 A4/A8/A9）

**Step 0.1**: 写脚本 dump `buildStaticIdentity('opus-47')` 非 pack 部分 vs `compileL0({catId:'opus-47'})`，逐段语义 diff
**Step 0.2**: 重点验证 A4（A2A 协作格式）/ A8（CVO handles）/ A9（governance digest 14 项）—— L0 是否语义等价覆盖（不是字面，是规则完整性）
**Step 0.3**: 输出 gap 清单。A8 CVO handles gap 确认 → Task 1 修；A4/A9 若有遗漏 → 补 L0（回 Phase B 补丁）
**Step 0.4**: Commit spike 结论 `docs/audits/2026-05-16-l0-coverage-diff.md`

### Task 1: 修 L0 CVO handles gap（A8）

**Files:** `scripts/compile-system-prompt-l0.mjs`, `assets/system-prompts/system-prompt-l0.md`, test
**Step 1.1**: Red 测试——compileL0 输出含 co-creator config 的 CVO handles（不是硬编码 @landy）
**Step 1.2**: 确认 Red（当前 L0 §4 硬编码）
**Step 1.3**: compile 注入 `{{CVO_REF}}` 模板变量（从 getCoCreatorConfig 渲染 ccName + ccHandles），L0 §4/§8 用占位
**Step 1.4**: Green + 全测试无 regression
**Step 1.5**: Commit

### Task 2: 剥离 params.systemPrompt 非 pack 部分（AC-C3）

> 🔴 **执行顺序约束（防回归窗口）**：Task 2 必须在 **Task 3 + Task 4 落地后**才执行（或同 PR 同落）。原因：Task 3/4 把 L0 经 `--system-prompt-file` / `-c` 接进 native system role（additive，安全，制造**临时**重复：L0 在 system role + 非 pack 仍在 user message）；Task 2 才删 user message 那份重复。顺序反了 = 有一段窗口非 pack 既不在 system role 也不在 user message = 猫丢身份/家规。对齐 CVO directive「接通之后再删重复」。
>
> **精确调用链（2026-05-16 逐行核对，防记错——`params.systemPrompt` **不是** `buildSystemPrompt`(L999) 来的）：**
> - `route-serial.ts:413` `staticIdentity = buildStaticIdentity(catId,{mcpAvailable,packBlocks})` →（同文件）`route-serial.ts:734` `...(staticIdentity ? { systemPrompt: staticIdentity } : {})` 传进 `invokeSingleCat`
> - `route-parallel.ts:173` 同样 `buildStaticIdentity(...)` + 镜像 systemPrompt 传参
> - `invoke-single-cat.ts:1114-1116` `effectivePrompt = injectSystemPrompt && params.systemPrompt ? ` `${params.systemPrompt}\n\n---\n\n${promptWithMission}` ` : ...` ← **这里把非 pack prepend 进 user message，要删的就是这份重复**
> - `invocationContext`（route-serial L487 build → L625/668 `parts` 数组 → `promptWithMission`）**已不在 systemPrompt 里，不动**
> - F-BLOAT：`invoke-single-cat.ts:1079-1088` resume 时不重复注入 `params.systemPrompt`（pack-only 仍沿用此逻辑；L0 走 `--system-prompt-file` 是 replace 模式每次传、无累积，天然免疫双注入）

**Files:**
- `packages/api/src/domains/cats/services/context/SystemPromptBuilder.ts`（新增导出 `buildStaticIdentityPackOnly(catId, options)`）+ `packages/api/src/domains/cats/services/index.ts`（re-export，对齐 L28 现有 `buildStaticIdentity` 导出）
- `packages/api/src/domains/cats/services/agents/routing/route-serial.ts:413`（`buildStaticIdentity` → `buildStaticIdentityPackOnly`）
- `packages/api/src/domains/cats/services/agents/routing/route-parallel.ts:173`（镜像）
- test: `test/system-prompt-builder.test.js`（守护测试，布偶猫规则必跑）+ route 层单测

**`buildStaticIdentityPackOnly` 接口契约**：只返回 buildStaticIdentity 内 5 个 pack block，**顺序固定** `masksBlock → workflowsBlock → guardrailBlock → defaultsBlock → worldDriverSummary`（对应 SystemPromptBuilder.ts L517/L561/L578/L583/L588），**不含任何非 pack**（identity/restrictions/`## 协作`/roster/WORKFLOW_TRIGGERS/CVO ref/GOVERNANCE_DIGEST/MCP 全部移交 L0 file）。无 pack blocks → 返回 `''`（route 层 `...(x ? {systemPrompt:x} : {})` 已处理空串）。

**Step 2.1**: Red——`buildStaticIdentityPackOnly('opus-47',{packBlocks})` 含 pack 内容、**不含**非 pack 锚点（断言不含 displayName 身份句 / `## 协作` / `Rule 0` / `cat_cafe_search_evidence`）；buildStaticIdentity 守护测试不回归
**Step 2.2**: 确认 Red（函数不存在）
**Step 2.3**: 实现——抽 buildStaticIdentity 5 个 pack push 分支为内部共享 helper，`buildStaticIdentityPackOnly` 复用之；buildStaticIdentity 原行为不变（守护测试绿）
**Step 2.4**: route-serial.ts:413 + route-parallel.ts:173 切 `buildStaticIdentityPackOnly`（systemPrompt 只剩 pack；非 pack 由已落地的 Task 3/4 `--system-prompt-file`/`-c` 进 system role）
**Step 2.5**: Green + 守护测试 `node --test test/system-prompt-builder.test.js` + F-BLOAT resume 不重复测试（AC-C4）
**Step 2.6**: Commit

### Task 3a: 共享 L0 编译边界 helper（Task 3/4 前置）

> 🔴 **架构决策（2026-05-16 核对，修正 plan 原"in-process writeL0File"假设）**：API 编译产物（`packages/api/dist`）**不能 in-process import** `scripts/compile-system-prompt-l0.mjs`——该 .mjs 硬编码 `await import('../packages/api/dist/config/cat-config-loader.js')`（相对自身路径），import 进 API 包会：① 把已构建包耦合到包外脚本 ② 要求 dist 已构建 ③ API 进程内二次 bootstrap catRegistry。**改走 subprocess 调 Phase B CLI**（KD-10 `writeL0File()` + `--out` 正是为此而建）——零模块图耦合、进程隔离、carrier 本就 spawn `claude`（5-15s）多一个短 node 子进程是噪音。技术 OQ，47 自决（KD-10 约束满足，无价值/scope 变更）。

**Files:**
- Create: `packages/api/src/domains/cats/services/agents/providers/l0-compiler.ts`
- Test: `packages/api/test/l0-compiler.test.js`

**接口契约**：
- `resolveL0CompilerScriptPath(cwd = process.cwd()): string | undefined` —— 镜像 `resolveDefaultClaudeMcpServerPath` 多候选模式（`scripts/...` / `../../scripts/...` / `../scripts/...`），返回首个 existsSync 命中或 undefined
- `compileL0ViaSubprocess(opts: { catId, outPath?, spawnFn?, cwd? }): Promise<string>` —— `outPath` 给 → spawn `node <script> --cat <catId> --out <outPath>`（Claude `--system-prompt-file` 用文件）；`outPath` 不给 → 捕获 stdout 返回 compiled L0 字符串（Codex `-c developer_instructions=` 用内联串）。`spawnFn` 注入测试无关闭真 spawn
- **fail-closed**：script 不可解析 / 子进程非 0 退出 / stdout 空 → throw（终态 user message 无非 pack，L0 缺失 = 猫无身份/家规，比失败 invocation 更糟；对齐铁律哲学 + KD-5 无 flag）

**Step 3a.1**: Red——`resolveL0CompilerScriptPath` 在 fake cwd 命中候选；`compileL0ViaSubprocess` 注入 fake spawn 返回 stdout 串 / 写 outPath；失败路径 throw
**Step 3a.2**: 确认 Red（模块不存在）
**Step 3a.3**: 实现 `l0-compiler.ts`
**Step 3a.4**: Green
**Step 3a.5**: Commit

### Task 3: ClaudeBgCarrierService argv --system-prompt-file（AC-C1）

**Files:** `packages/api/src/domains/cats/services/agents/providers/ClaudeBgCarrierService.ts`（`startJob` argv，L191 附近）, test
**Step 3.1**: Red——spawn argv 含 `--system-prompt-file <tmpPath>`，tmpPath 内容 = `compileL0(this.catId)` 渲染（注入 fake l0CompilerFn）
**Step 3.2**: 确认 Red
**Step 3.3**: 实现：`startJob` spawn `claude` 前 `await compileL0ViaSubprocess({ catId: this.catId, outPath: tmpPath })`（tmpPath = `mkdtempSync(join(tmpdir(),'cat-cafe-l0-'))`/`system-prompt-l0.md`，per-invocation 不复用）→ `args.push('--system-prompt-file', tmpPath)`。fail-closed：compile throw 则 `finish(new CarrierError(...))`。新增 `l0CompilerFn?` 注入 seam（默认 `compileL0ViaSubprocess`），对齐现有 `spawnFn` seam
**Step 3.4**: Green + e2e（bg + system-prompt-file + mcp-config 三件套，复用 S0 spike 方式）
**Step 3.5**: Commit

### Task 4: CodexAgentService -c developer_instructions（AC-C2）

**Files:** `packages/api/src/domains/cats/services/agents/providers/CodexAgentService.ts`（spawn argv）, test
**Step 4.1**: Red——argv 含 `-c developer_instructions=<compileL0(this.catId)>`（注入 fake l0CompilerFn 返回固定串），并发安全（不写 `~/.codex/config.toml`，per-call argv）
**Step 4.2**: 确认 Red
**Step 4.3**: 实现：spawn 前 `const l0 = await compileL0ViaSubprocess({ catId: this.catId })`（无 outPath → stdout 串）→ argv push `-c` `developer_instructions=<l0>`（对齐 S4 砚砚 `62b9255e2` per-call 路径）。fail-closed 同 Task 3
**Step 4.4**: Green + 并发测试（@codex/@gpt52/@spark 各自 catId 独立 argv，互不污染）
**Step 4.5**: Commit

### Task 5: 全量门禁 + runtime 重启验收（AC-C5）

**Step 5.1**: `pnpm gate`（无 NODE_ENV 前缀——脚本内部 env -u 自处理，前缀污染 next build）
**Step 5.2**: quality-gate（47 盲审规则：opus-47 作者 → 砚砚执行）
**Step 5.3**: request-review 砚砚（本地）→ 修反馈 → 云端 review
**Step 5.4**: merge-gate → squash merge
**Step 5.5**: runtime 重启验收：47/46/砚砚 各跑一轮（确认 L0 在 system role + invocationContext 在 user message + 无双重重复）+ 铲屎官 10 轮压缩对话（客观性终验：工具/safety/并行/压缩感知不退化）
**Step 5.6**: 不行 → `git revert <merge commit>` + runtime 重启回滚（CVO directive，3 分钟）

---

## Open Questions

### 技术 OQ（实施自决）
- OQ-1: SystemPromptBuilder 拆 `buildStaticIdentityPackOnly()` 接口——改 SystemPromptBuilder = 触发守护测试，Task 2 跑 `node --test test/system-prompt-builder.test.js`
- OQ-2: tmpPath 生命周期——per-invocation mkdtemp，invoke 结束清理；daemon resume 是否需持久化路径？Task 3 spike 确认（F-BLOAT 关联）

### 价值 OQ（CVO）
- 无。placeholder + 不灰度 + git revert 回滚已 CVO 拍板（本 thread 2026-05-16）。回滚成本低（git revert + runtime 重启 3 分钟）。

---

## 回滚

`git revert <Phase C merge commit>` + runtime 重启。CVO directive：不灰度不 feature flag，git log 回滚足够。Phase C 改 invocation 链路但纯 prompt 拼装（无数据迁移/schema 变更），revert 干净。
