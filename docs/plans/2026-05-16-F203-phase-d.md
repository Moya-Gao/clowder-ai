# F203 Phase D — Root md 瘦身 Implementation Plan

**Feature:** F203 — `docs/features/F203-native-system-prompt-l0.md`
**Goal:** L0 切到 native system role 后，CLAUDE.md / AGENTS.md 删掉已被 L0 覆盖（或可从代码/文档重建）的重复内容，瘦到 ≤65 行，保留 harness-specific 且 L0 不含的内容。
**Acceptance Criteria（F203 spec Phase D）:**
- AC-D1: CLAUDE.md ≤ 65 行（当前 200）
- AC-D2: AGENTS.md ≤ 65 行（当前 219）
- 单独行动：root md 删队友静态表（SystemPromptBuilder 动态生成，静态副本是漂移源）
- 验证：跑一次实际 invocation，确认压缩后 L0 14 项规则仍在 system prompt + user message 显著瘦身
**Architecture cell:** harness/system-prompt-injection（与 Phase C 同 cell）
**Map delta:** none（不改注入通道，只删 root md 里已被 L0 覆盖的重复文字；Phase C 已记通道变更）
**Map delta why:** Phase D 是纯文档瘦身，注入链不变（L0 已在 Phase C 接通）。无新增 Store/Queue/Router。
**Architecture:** CLAUDE.md/AGENTS.md 是 **harness 级 project context**（Claude Code / Codex CLI 启动读）；L0 是 **per-invocation native system prompt**（Phase C 接通，压缩免疫）。Phase D 删 root md 里「已在 L0 可靠覆盖」或「ADR-030 §10.3 可压缩（代码/文档可重建）」的段，只保留 harness-specific 且 L0 不含的内容（闭环检查点 / 各族专属 dev 规则）。
**Tech Stack:** Markdown only（CLAUDE.md / AGENTS.md）+ node:test 守护测试 + 实际 invocation 验证
**前端验证:** No（纯 root md 文档 + invocation 行为验证）

---

## 🔴 精确保留/删除清单（防"干着忘记"，对照 L0 truth source `assets/system-prompts/system-prompt-l0.md` 163 行 + ADR-030 §10.3）

### CLAUDE.md（200 行）逐段处置

| # | 段（行号近似）| L0 是否覆盖 | 处置 | 理由 |
|---|---|---|---|---|
| C1 | 你是谁/identity (L1-11) | ✅ L0 §1 IDENTITY_BLOCK（compile per-cat 注入）| **删→1 行指针** | L0 是 identity 真相源（per-cat overlay）；root md 留 1 行"身份见 native system prompt L0 §1" |
| C2 | 队友静态表 (L13-24) | ✅ L0 §1 TEAMMATE_ROSTER（SystemPromptBuilder 动态生成）| **删**（spec 单独行动）| 静态副本是漂移源（ADR-030 §10.3）；动态 roster 才反映 runtime catalog |
| C3 | SOP 导航表 (L26-56) | ⚠️ L0 §3 P2「自主跑完 SOP」+ §6 WORKFLOW_TRIGGERS（per-breed 触发点）| **删表→留指针** | 完整 SOP 链真相源 `docs/SOP.md`；Skill 加载自带其指令（ADR-030 §10.3）。留 1 行"完整流程 docs/SOP.md，Skill 适用必加载" |
| C4 | 五条铁律 (L58-65) | ✅ L0 §5 五条铁律（逐条同源）| **保留（terse）** | spec 明确"保留五条铁律"。CLAUDE.md 是 harness 启动**第一**读，Redis 6399 圣域等 P0 安全在 invocation context 前就要在场——保留是 defense-in-depth，非冗余 |
| C5 | 记忆系统详述 (~L66-148, ~80 行) | ✅ L0 §7 MCP quick index（search/graph/recent 三入口）+ §8 上下文管理 + **session hook 每轮注入三入口提示** | **删详述→留指针** | `cat-cafe-skills/refs/memory-routing-partial.md` 是真相源；session hook 每轮已注入三入口（ADR-030 §10.3）。留 1 行"记忆三入口见 L0 §7 / partial" |
| C6 | Knowledge Feed 完整段 | ✅ L0 §3 W7（Knowledge Feed 自动提取，主动澄清+提醒）| **删**（W7 已覆盖核心）| L0 W7 已含"猫不写标签+主动澄清+提醒铲屎官看 Feed"；API 细节 `docs/` 可重建 |
| C7 | 流程闭环检查点 (L149-156) | ❌ L0 不含（harness-specific：压缩后自检/改共享文档即时 commit/feat close 通知）| **保留** | L0 是 cat identity/家规，这段是 harness 工作流检查点——L0 不该含，root md 是正确位置 |
| C8 | 布偶猫专属规则 (L157-182) | ❌ L0 不含（LSP 诊断/Redis 测试隔离/JetBrains MCP/SystemPromptBuilder 守护测试）| **保留** | 全是 harness/工具链 dev 规则（typescript-lsp 插件、test:redis、mcp__jetbrains__*），L0（注入给 runtime 猫）不该含 |
| C9 | 代码规范速查 (L183-190) | ⚠️ L0 §3 质量覆盖（方向/质量原则）| **删→留指针** | 具体阈值（200 行警告/biome 命令）真相源 `docs/SOP.md`「代码质量工具」（ADR-030 §10.3） |
| C10 | 关键文档表 (L191-200) | ❌ | **删** | `ls docs/` + memory search 可重建（ADR-030 §10.3） |

**CLAUDE.md 结论**：保留 C4（terse 五条铁律）+ C7（闭环检查点）+ C8（布偶猫专属规则）+ 顶部极简 identity 1 行 + C3/C5/C9 各 1 行指针。删 C1 详述/C2/C3 表/C5 详述/C6/C10。预计 ~55-60 行。

### AGENTS.md（219 行）逐段处置 — 缅因猫版，同构

| # | 段 | 处置 | 理由 |
|---|---|---|---|
| A1 | 你是谁 + 身份契约 (L1-15) | **删→1 行指针** | 同 C1，L0 §1 缅因猫 IDENTITY_BLOCK 真相源 |
| A2 | 队友静态表 (L16-28) | **删** | 同 C2 |
| A3 | SOP 导航 (L29-61) | **删→留指针** | 同 C3 |
| A4 | 执行纪律 (L62-72) | **保留（terse）** | 缅因猫 harness 纪律，评估是否 L0 §3 纪律已覆盖；重叠则降为指针，不重叠保留 |
| A5 | 四条铁律 (L73-79) | **保留（terse）** | 同 C4（缅因猫四条铁律，L0 §5 对应）|
| A6 | 记忆系统 (L80-159, ~80 行) | **删详述→留指针** | 同 C5 |
| A7 | Knowledge Feed | **删** | 同 C6 |
| A8 | 缅因猫专属规则 (L160-210)：角色切换自检/Review 方法论/严重度定义/代码质量红线/安全审查重点/Git 安全/Codex 沙盒 | **保留** | 同 C8——全是 reviewer/codex harness 专属（severity 定义、SSRF/DoS 审查重点、codex sandbox），L0 不含，root md 是正确位置 |
| A9 | 关键文档表 (L211-219) | **删** | 同 C10 |

**AGENTS.md 结论**：保留 A4（评估后 terse）+ A5（terse 四条铁律）+ A8（缅因猫专属规则，review/安全/codex 沙盒）+ 极简 identity 1 行 + 指针。预计 ~55-60 行。

### 一句话总结（压缩后自读用）

> **删**：identity 详述/队友静态表/SOP 表/记忆详述/Knowledge Feed 段/代码规范详表/文档表（L0 已覆盖 OR ADR §10.3 可重建）。
> **留**：terse 铁律（harness 第一读 P0 安全）+ 闭环检查点 + 各族专属 dev/工具链规则（L0 不含的 harness-specific）+ 关键指针 1 行化。
> 删错 = 猫丢 harness 工作流/工具链规则（L0 不会补，因为 L0 是 runtime identity 不是 dev harness 上下文）。

---

## Straight-Line Check

- **B 定义**：CLAUDE.md ≤65 行 / AGENTS.md ≤65 行，只含 harness-specific 且 L0 不覆盖的内容 + 极简指针；删掉的内容在 L0（compile 注入）或 docs/代码（可重建）里仍可靠存在。
- **终态 schema**：root md 结构 = 极简 identity 指针 → terse 铁律 → 闭环检查点 →（族）专属 dev 规则 → 指针段（SOP/记忆/代码规范/文档各 1 行）。
- **NOT building**：不改 L0 内容、不改注入链（Phase C 已接通）、不改 SystemPromptBuilder、不动 docs/SOP.md 等真相源、不删 harness-specific 段。

---

## Tasks

> 🔴 **执行顺序**：Task 1（守护测试 Red）→ Task 2（CLAUDE.md 瘦身 Green）→ Task 3（AGENTS.md 瘦身 Green）→ Task 4（invocation 验证 L0 14 项规则在场 + user message 瘦身）→ Task 5（gate + review + merge）。守护测试先行，防瘦身误删 keep-anchor。

### Task 1: root md 瘦身守护测试（Red）

**Files:** Create `packages/api/test/root-md-slim.test.js`（或就近 test 目录），node:test
**Step 1.1**: Red 测试断言：
- `wc -l CLAUDE.md` ≤ 65；`wc -l AGENTS.md` ≤ 65
- CLAUDE.md **含** keep-anchor：`Redis 6399 圣域`（铁律）、`流程闭环检查点`、`SystemPromptBuilder 守护测试`（布偶猫专属）、`typescript-lsp`
- CLAUDE.md **不含** cut-section 标志：`## 队友`（静态表）、`SOP 导航`完整表、记忆系统 ~80 行详述（断言无 `### 检索策略`）
- AGENTS.md 含 `严重度定义` / `Codex 沙盒`；不含静态队友表 / 记忆详述
- CLAUDE.md/AGENTS.md **含指针**：`docs/SOP.md`、`memory-routing-partial.md`
**Step 1.2**: 确认 Red（当前 200/219 行，断言 fail）
**Step 1.3**: Commit 测试

### Task 2: CLAUDE.md 瘦身（Green）

**Files:** Modify `CLAUDE.md`（200→~58 行）
**Step 2.1**: 按上表删 C1 详述/C2/C3 表/C5 详述/C6/C9 详表/C10；保留 C4 terse + C7 + C8 + 极简 identity + 指针段
**Step 2.2**: 跑守护测试 → CLAUDE.md 部分 Green
**Step 2.3**: Commit（共享文档，立即 commit+push）

### Task 3: AGENTS.md 瘦身（Green）

**Files:** Modify `AGENTS.md`（219→~58 行）
**Step 3.1**: 按上表同构瘦身（A4 执行纪律先 diff L0 §3 纪律重叠度再定 terse/指针）
**Step 3.2**: 守护测试全 Green
**Step 3.3**: Commit + push

### Task 4: invocation 验证（spec 硬要求）

**Step 4.1**: 跑实际 invocation（worktree 隔离 / 或 compile-system-prompt-l0 输出检查），确认 L0 14 项规则仍在 system prompt（compile 输出含 Rule 0/P1-P5/W1-W8/Magic Words/五条铁律/传球三选一）
**Step 4.2**: 确认 root md 瘦身后 user message 层无 L0 重复（buildStaticIdentity 非 pack 仍由 Phase C 通道走 native，CLAUDE.md 不再是 L0 副本源）
**Step 4.3**: 记录验证证据到 plan/spec

### Task 5: 全量门禁 + review + merge

**Step 5.1**: `pnpm gate`
**Step 5.2**: quality-gate（47 盲审 → 砚砚执行）
**Step 5.3**: request-review 砚砚（本地）→ 修 → 云端 review（root md 是否豁免云端？——CLAUDE.md/AGENTS.md 非 `cat-cafe-skills/`，不在 merge-gate 豁免清单，**走云端**）
**Step 5.4**: merge-gate squash merge + Phase 文档同步

## Open Questions

### 技术 OQ（实现中自决）
- A4 缅因猫执行纪律 vs L0 §3 纪律重叠度——Task 3 时逐行 diff，重叠则降指针，不重叠 terse 保留
- 极简 identity 行措辞——指向 L0 §1 即可，1 行

### 价值 OQ（CVO）
无。瘦身范围 spec 已定（删 X 留 Y），回滚成本低（`git revert` 单 md commit）。CVO 已就方向"按 d e f 做"拍板。

## 回滚

`git revert <merge commit>`——纯 md，无运行时影响（L0 注入链 Phase C 已独立验证）。
