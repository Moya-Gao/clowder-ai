---
feature_ids: [F231]
related_features: [F128, F225, F102]
topics: [user-profile-capsule, phase-c-nurturing-loop, profile-update-proposal, stateful-object-gate]
doc_kind: plan
created: 2026-06-14
---

# F231 Phase C AC-C1 养熟循环最小闭环 Implementation Plan

**Feature:** F231 — `docs/features/F231-user-profile-capsule.md`
**Goal:** 跑通养熟循环的第一个真实闭环——关系信号 → capsule/primer 更新提议 → CVO 签字 → 写入 per-cat primer + provenance（三段管道 KD-8 的 push→pull 写入路径，AC-C1）。
**Acceptance Criteria（本 plan 覆盖 AC-C1，C2/C3 后续 plan）：**
- AC-C1: 关系信号→capsule/primer 更新提议路径落地（三段管道 KD-8，KD-12 分层写入制），至少 1 次真实更新走完全程（跑在白名单采集 + runtime-neutral trigger 真骨架上，非 L0 反射脚手架）
**Architecture cell:** identity-session（subcell identity-user-profile，F231 owns）
**Map delta:** none（复用 F128 ProposalStore 状态机模式 + F102 evidence lane；不新建 cell）
**Map delta why:** Phase C 实现挂在 identity-user-profile 既有边界内（capsule/primer 写入），proposal 基础设施复用 F128，无新 ownership 边界。
**Architecture:** 复用 F128 propose→approve 基础设施（ProposalStore 状态机 + proposal-card + decision route）。新增 `ProfileUpdateProposalStore`（仿 `SessionHandoffProposalStore`，profile-specific payload）+ MCP 工具 `cat_cafe_propose_profile_update`（仿 `propose_thread`）+ approve 后的写入 handler（写 per-cat primer + provenance）。AC-C1 只做**低代价 per-cat primer 写入路径**（KD-15：不碰 shared capsule，capsule 晋升是后续）。
**Tech Stack:** TypeScript, Redis (ProposalStore), Zod (MCP schema), Vitest/node test, fs (primer/provenance 写入)
**前端验证:** Yes — proposal 确认卡复用现有 proposal-card-block，reviewer 用 Playwright 实测 approve/reject。

---

## Straight-Line Check (A→B)

**B 定义（一句话）**：一只猫调 `cat_cafe_propose_profile_update`（带 before/after diff + 来源 + owner cat），CVO 在确认卡点 Approve，系统把更新写进 `private/profile/relationship/{catId}-primer.md` 并归档一条 provenance——全程可在真实 thread 走通一次。

**不做（AC-C1 范围外）**：
- ❌ shared capsule 写入（高代价路径，KD-15 晋升，C2/后续）
- ❌ 自动采集 + 蒸馏 trigger（KD-10 runtime 事件挂钩，C3）——AC-C1 是**猫主动声明**入口（KD-12 消化端手动入口，46 反射降级版）
- ❌ 注入层动态 recall（OQ-5，carve out）
- ❌ 用中校准的"使用→纠正"自动回流（KD-13，后续）——AC-C1 先做正向写入闭环

**Terminal schema（最终形态，步骤围绕它建）**：

```typescript
// packages/shared/src/types/profile-update.ts (new)
export type ProfileUpdateTargetLayer = 'primer';          // AC-C1 only; 'capsule' added in C2
export type ProfileUpdateProposalStatus = 'pending' | 'approving' | 'approved' | 'rejected';

export interface ProfileUpdateProposal {
  proposalId: string;
  status: ProfileUpdateProposalStatus;
  sourceThreadId: string;
  sourceInvocationId: string;
  sourceCatId: CatId;          // owner cat (primer is per-cat)
  targetLayer: ProfileUpdateTargetLayer;
  // payload
  targetPath: string;           // resolved primer file path (per-cat), pinned at propose time
  beforeContent: string;        // current primer content at propose time (for diff + audit)
  baseContentHash: string;      // P1-2 optimistic lock: hash of current primer at propose; approve re-reads & compares before write (no silent overwrite of a concurrently-changed primer)
  afterContent: string;         // proposed new content
  rationale: string;            // why this update (shown on card)
  signalProvenance: {           // KD-9 whitelist source — where the signal came from
    kind: 'cat-declared' | 'cvo-instructed';   // AC-C1: manual entry only (no classifier)
    sourceMessageId?: string;
    sourceThreadId: string;
  };
  createdBy: string;
  createdAt: number;
  // lifecycle (mirrors ThreadProposal)
  approvedBy?: string; approvedAt?: number; claimedAt?: number;
  rejectedBy?: string; rejectedAt?: number; rejectionReason?: string;
  // P1-1 partial-commit checkpoint: BOTH recorded BEFORE finalize via recordCheckpoint({writtenPath, provenancePath}).
  // Deterministic paths (proposalId-based) → both writes are overwrite-idempotent on retry.
  writtenPath?: string;         // primer written (checkpointed before finalize)
  provenancePath?: string;      // provenance written (checkpointed before finalize; deterministic path = retry-safe)
  cardMessageId?: string;
}
```

---

## Stateful Object Gate（F229 教训：状态机先行，不留边给 reviewer 逐轮补）

### Census — 本 plan 的生命周期对象

1. **ProfileUpdateProposal**（核心状态机，下方三件套）
2. **primer 文件**（被写对象——但写入是 last-write-wins 覆盖 + provenance 留痕，非独立状态机；不变量见 INV-5/6）
3. **dedup reservation**（clientRequestId → proposalId，幂等，复用 ProposalStore 模式）

> 复用决策：ProfileUpdateProposal 状态机**完全镜像** F128 `ThreadProposal`（pending→approving→approved/rejected + 崩溃恢复 + 幂等），因为 review 过的边都一样。差异只在 payload（profile content）和 approve 动作（写文件 vs 建 thread）。**不重新发明状态机**——直接复用 `IProposalStore` 接口形状，新建 `ProfileUpdateProposalStore`（仿 `SessionHandoffProposalStore`）。

### 1. 状态 × 事件转移表

| 当前态 | 事件 | 新态 | 操作 | lifecycle owner |
|--------|------|------|------|----------------|
| (none) | propose | `pending` | create + reserveDedup + 发卡 | propose route |
| `pending` | CVO approve（baseContentHash match） | `approving` | claimForApproval (CAS) + 重读 primer 比对 baseContentHash | decision route |
| `pending` | CVO approve（hash **MISMATCH**） | `pending` | 不进 approving — 拒绝 + 提示 stale/supersede（primer 已被并发提议改，不静默覆盖，P1-2） | decision route |
| `approving` | 写 primer + 写 provenance（deterministic paths） | `approving` | recordCheckpoint({writtenPath, provenancePath}) — **两个都 checkpoint BEFORE finalize**（P1-1 双窗口） | decision route |
| `approving` | finalize | `approved` | finalizeApproval (CAS)（primer + provenance 都已落盘+checkpoint，finalize 只翻状态） | decision route |
| `approving` | 写失败 | `pending` | rollbackClaim (CAS) | decision route |
| `pending` | CVO reject | `rejected` | markRejected (CAS) | decision route |
| `pending` | propose 同 clientRequestId | `pending` | 返回同 proposalId（幂等，不新建） | propose route |

**唯一 lifecycle owner**：propose route 建、decision route 转。**旁路 API 禁止**：generic delete 只在 propose partial-commit 失败时清理（卡发送失败），禁止在 approved 后 delete（provenance 是 immutable 审计）。

### 2. 不变量清单

- **INV-1**：写 primer 文件**只在** status 进入 `approving` 之后（CVO 签字红线 KD-12，绝不在 pending 写）。可测：pending proposal 调写入 → 拒绝 + 文件不变。
- **INV-2**：`writtenPath` **和** `provenancePath` 都在各自写成功后、finalize 前 checkpoint（P1-1 partial-commit 幂等恢复——进程在任一步和 finalize 间死，恢复凭 checkpoint 判断哪步已做、不重复）。可测：mock finalize 前 crash（primer 已写 / provenance 已写两种）→ 恢复读 checkpoint → 不二次写。
- **INV-3**：approve 是 CAS `pending→approving`，并发双 approve 只有一个成功（claimForApproval 返回 null 给 loser）。可测：并发两次 claim 同 proposalId → 恰一个非 null。
- **INV-4**：同 (userId, clientRequestId) 幂等——重复 propose 返回同 proposalId，不产生第二个 proposal（reserveDedup）。可测：连发两次同 clientRequestId → 同 proposalId。
- **INV-5**：provenance 文件 immutable——一旦写入永不改/删（审计 trail）。可测：approved 后无删除路径触达 provenance。
- **INV-6**：primer 写入是低代价 per-cat 路径（targetLayer='primer'），AC-C1 **禁止** targetLayer='capsule'（schema 层拒绝）。可测：capsule target → 400 拒绝（C2 才开）。
- **INV-7**：provenance 写入用 deterministic path（proposalId-based）→ retry 覆盖同文件、不产生重复 provenance（P1-1 第二窗口：provenance 写后 finalize 前死，retry 不 dup）。可测：同 proposalId 重复写 provenance → 单文件。
- **INV-8**：approve 写 primer 前重读 primer current hash 必须 == `baseContentHash`（propose 时锁的）；mismatch 拒绝、绝不覆盖（P1-2 乐观锁，防并发提议互盖）。可测：propose 后 primer 被改 → approve hash mismatch → 拒绝 + primer 不变。

### 3. 对抗场景（每个一条测试）

- **ADV-1 崩溃窗口（双）**：(a) 写 primer 成功→死→恢复：凭 `writtenPath` checkpoint 判已写，补写 provenance（deterministic 幂等）→ re-finalize，primer 不二次覆盖。(b) primer+provenance 都写成功→finalize 前死→恢复：凭两个 checkpoint 判都已写，直接 re-finalize（不重写任何文件、provenance 不 dup）。
- **ADV-2 并发双 approve**：两个 CVO 操作同时 approve 同 proposal。期望：claimForApproval CAS 只让一个进 approving，另一个收 null（已处理提示）。
- **ADV-3 写文件失败回滚**：approve 后写 primer 抛错（磁盘/权限）。期望：rollbackClaim 回 pending，卡可重试，无半截 provenance。
- **ADV-4 stale propose**：猫连发两次（网络重试）同 clientRequestId。期望：reserveDedup 返回同 proposalId，单卡单 proposal。
- **ADV-5 reject 后 approve**：proposal 已 rejected，再来 approve。期望：claimForApproval 返回 null（status 非 pending），不写文件。
- **ADV-6 两提议同 primer（P1-2 乐观锁）**：proposal-X 和 proposal-Y 都 pending、targetPath 同一 `{catId}-primer.md`、同 baseContentHash。先 approve X（primer 变、hash 变）→ 再 approve Y：Y 的 baseContentHash ≠ current → 拒绝（stale/supersede），**绝不覆盖 X 的写入**。期望测试：两 proposal 序列 approve，第二个被乐观锁挡下，primer = X 的内容。

---

## Tasks（AC-C1 TDD 分步）

### Task 1: ProfileUpdateProposal 类型 + Store（复用 ProposalStore 模式）
**Files:**
- Create: `packages/shared/src/types/profile-update.ts`（terminal schema 上方）
- Create: `packages/api/src/domains/cats/services/stores/ports/ProfileUpdateProposalStore.ts`（仿 `ProposalStore.ts`，InMemory + interface）
- Create: `packages/api/src/domains/cats/services/stores/redis/RedisProfileUpdateProposalStore.ts`（仿 `RedisProposalStore.ts`）
- Test: `packages/api/test/profile-update-proposal-store.test.ts`

**TDD：** 红测先覆盖 INV-1~8 + ADV-1~6（状态机 + 双崩溃窗口 + 幂等 + 乐观锁），再实现。Redis 测试用 `pnpm --filter @cat-cafe/api test:redis`（INV-2/7 + ADV-1 双崩溃窗口必须 Redis-backed，in-memory 测不到——feedback_inmemory_store_tests_miss_redis_behavior）。

### Task 2: 写入 + provenance handler（KD-12/15 低代价 per-cat 路径）
**Files:**
- Create: `packages/api/src/domains/cats/services/profile/writeProfileUpdate.ts`（重读 primer 比对 `baseContentHash` → 写 `{catId}-primer.md` → 写 **deterministic-path** provenance（proposalId-based）→ 返回 `{writtenPath, provenancePath}` 供 decision route checkpoint）
- Test: `packages/api/test/write-profile-update.test.ts`（用 fixture profileDir，不碰真实 private/）

**TDD：** 红测覆盖：写前后 primer 内容 + provenance 存在/格式（仿 Phase B 三段式）+ **INV-8 hash mismatch 拒绝**（ADV-6 并发互盖）+ **INV-7 provenance deterministic 幂等**（重写不 dup，ADV-1b）+ INV-1（只 approving 后写）+ ADV-3（写失败不留半截）。

### Task 3: 云端 propose + decision routes
**Files:**
- Create: `packages/api/src/routes/callback-propose-profile-update-routes.ts`（仿 `callback-propose-thread-routes.ts`）
- Modify: `packages/api/src/routes/proposal-routes.ts` 或新增 decision route（approve→claim→writeProfileUpdate→recordWrittenPath→finalize→provenance / reject→markRejected）
- Test: route 集成测试

### Task 4: MCP 工具 cat_cafe_propose_profile_update
**Files:**
- Modify: `packages/mcp-server/src/tools/callback-tools.ts`（加 schema + handler，仿 `handleProposeThread` line 1569-1606，callbackPost('/api/callbacks/propose-profile-update')）
- Modify: `packages/mcp-server/src/tools/index.ts`（导出）
- Test: MCP 工具单测

### Task 5: 确认卡（复用 proposal-card-block）
**Files:**
- Modify: `packages/api/src/routes/proposal-card-block.ts`（加 profile-update 卡变体，含 before/after diff 展示 + approve/reject 按钮）
- 前端 reviewer Playwright 实测

### Task 6: AC-C1 真实 dogfood（CVO 出镜）
- 找一个真实关系信号 → 猫调 propose_profile_update → CVO（铲屎官）在卡上 Approve → 验证 primer + provenance 落地。**这步 @landy。**

---

## Open Questions

**技术 OQ（实现中自决）：**
- TOQ-1: ProfileUpdateProposalStore 复用 RedisProposalStoreHelpers 多少？（看 helper 是否 thread-specific）
- TOQ-2: decision route 是新建还是扩展现有 proposal-routes 的 approve 分发？（看现有 approve 怎么按 proposal type 分发）

**价值 OQ（CVO）：**
- VOQ-1（AC-C1 dogfood）：用哪个真实关系信号做第一次闭环？需要 CVO 提供 + 签字。Task 6 @landy。

---

## 下一步

plan committed → 加载 `worktree`（隔离环境 + Redis 6398）→ `tdd`（Task 1 红测起）。
