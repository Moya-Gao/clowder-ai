---
feature_ids: [F246, F231]
topics: [approval-hub, adapter, profile-update]
doc_kind: plan
created: 2026-06-22
---

# F246 v2: F231 ProfileUpdateProposal Adapter

**Feature:** F246 — `docs/features/F246-approval-hub.md`
**Goal:** Add F231 ProfileUpdateProposal as 4th adapter to the Approval Hub, completing v2 admission.
**Acceptance Criteria:**
- AC-V2-1: `ApprovalFeatureId` union includes `'F231'`
- AC-V2-2: `F231ApprovalAdapter` maps pending `ProfileUpdateProposal` to `ApprovalItem` (jump-only, `inlineApprovable=false`)
- AC-V2-3: Adapter registered in `approval-hub-routes` setup → F231 proposals visible in Hub
- AC-V2-4: Tests cover: mapping correctness, empty user, requesterCatId, detail fields, stale threshold, cardMessageId
- AC-V2-5: Hub frontend renders F231 items (no frontend code change needed — generic `ApprovalItemCard` handles all feature IDs, jump-only cards use existing teleport path)
**Architecture cell:** platform-infra (subcell: `approval-index`)
**Map delta:** none
**Map delta why:** Adding a 4th adapter to existing Hub infrastructure, no cell boundary change.
**Tech Stack:** TypeScript, Fastify, node:test
**前端验证:** No — frontend is generic (renders any `ApprovalFeatureId`). No UI code changes.

---

## Straight-Line Check

**Finish line:** F231 `propose_profile_update` pending items appear in the Approval Hub, jump-only (no inline approve). CVO can see profile update proposals across all threads.

**Not building:** Inline approve for F231 (primer diff requires thread context — same rationale as F225). No materialized index (4 adapters < 5-adapter threshold per AC-D7).

## Implementation

### Task 1: Extend `ApprovalFeatureId` union type

**Files:**
- Modify: `packages/shared/src/types/approval-hub.ts:11`

**Step 1:** Change `ApprovalFeatureId` from `'F128' | 'F225' | 'F193'` to `'F128' | 'F225' | 'F193' | 'F231'`

**Step 2:** Run `pnpm --filter @cat-cafe/shared build` to confirm no type errors.

**Step 3:** Commit: `feat(F246): add F231 to ApprovalFeatureId union`

### Task 2: Write F231ApprovalAdapter (TDD)

**Files:**
- Create: `packages/api/test/approval-hub/f231-approval-adapter.test.js`
- Create: `packages/api/src/domains/approval-hub/adapters/F231ApprovalAdapter.ts`

**Step 1: Write failing tests**

Test cases (mirrors F225 adapter test):
1. Maps pending ProfileUpdateProposals to ApprovalItems (sourceFeatureId='F231', status='pending', inlineApprovable=false)
2. Computes expiresAt as createdAt + 7 days (profile updates less urgent than session handoffs)
3. Returns empty for user with no pending proposals
4. Sets requesterCatId from sourceCatId
5. Includes rationale, targetLayer, targetPath in detail
6. Maps cardMessageId to sourceMessageId

**Step 2: Run tests → all RED**

**Step 3: Write minimal adapter**

Pattern: identical to `F225ApprovalAdapter.ts`, substituting:
- Import `ProfileUpdateProposal` + `IProfileUpdateProposalStore`
- `featureId = 'F231'`
- `store.listPending(userId)` (same method name, same semantics — 砚砚 confirmed)
- `toItem()` maps: `summary = "Profile update: ${p.rationale.slice(0, 60)}"`, `detail = { rationale, targetLayer, targetPath, beforeContent (truncated), afterContent (truncated), signalProvenance }`
- `ownerUserId = p.createdBy` (confirmed)
- `requesterCatId = p.sourceCatId`
- `inlineApprovable = false` (jump-only, needs thread context)
- Stale: 7 days (same as F128, profile changes aren't urgent)

**Step 4: Run tests → all GREEN**

**Step 5:** Commit: `feat(F246): add F231ApprovalAdapter with tests`

### Task 3: Register adapter in API startup

**Files:**
- Modify: `packages/api/src/index.ts` (import + add to adapters array)

**Step 1:** Add import: `import { F231ApprovalAdapter } from './domains/approval-hub/adapters/F231ApprovalAdapter.js';`

**Step 2:** Add to adapters array at ~line 2606: `new F231ApprovalAdapter(profileUpdateProposalStore),`

**Step 3:** Run existing `approval-hub-routes.test.js` to confirm aggregation works with 4 adapters.

**Step 4:** Commit: `feat(F246): register F231 adapter in approval hub routes`

### Task 4: Update feature doc + build verification

**Files:**
- Modify: `docs/features/F246-approval-hub.md` (Census table, status)

**Step 1:** Update Census table: F231 row from "v2 候选" to "**v2**"

**Step 2:** Run full gate: `pnpm test && pnpm check && pnpm lint`

**Step 3:** Commit: `docs(F246): mark F231 as v2 active in census`

## Open Questions

None — all technical questions resolved via砚砚 cross-thread coordination (store API stable, userId semantics confirmed, interface identical to F225).
